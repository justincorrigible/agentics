Live-tested the global-dispatch pattern from yesterday by actually starting a session in an adopting project (Arranger): the session ran the generic checklist (roadmap, tech-debt, git log on instruction files) but skipped every agentics-specific step, and had no awareness of the new `.dev/sessions/` convention at all when asked directly. Root cause: the dispatch text in personal global context cited agentics as "the base convention" instead of instructing a fresh read, so the agent fell back to habit rather than fetching current content.

- `~/.claude/CLAUDE.md` (outside this repo): rewrote Session discipline from a citation to an imperative: read the local agentics clone's `session-discipline.md` fresh at every session-start signal, using a direct path instead of routing through the `projects.md` lookup
- `template/conventions/convention-levels.md`: generalized the fix into the template's own "Global" guidance, so other adopters setting up the same dispatch pattern don't hit the same silent failure
- `CHANGELOG.md`: logged `dispatch-must-be-imperative` under Unreleased

Checked Usher (a later, independently-written project) before testing there and found it has zero textual link to agentics at all, despite reimplementing an almost identical session-start checklist: a third distinct gap from the two above, since there's no tag or mention to detect regardless of how the detection logic is written. Formalized the response to this and to the ongoing ad-hoc migration prompts (Arranger) into a reusable procedure rather than hand-writing a new one per project each time.

- `template/conventions/upgrading-adoption.md`: new file: on-demand procedure covering adoption confirmation (including the no-textual-link case), diagnosis against the current template (tag, session files, `AGENTS.md` completeness, agent-neutrality), and consent-gated application
- `template/conventions/convention-levels.md`: § Checking for upstream updates now defers to the new file for the "tag missing or stale" case instead of describing the fix inline, avoiding two copies of the same logic
- `template/CLAUDE.md`, `template/AGENTS.md`: wired in a dispatch entry and an inline pointer section respectively
- `CHANGELOG.md`: logged `upgrading-adoption` under Unreleased

Audited the whole repo (not just `template/`) for personal identifying details on request, and found real leaks from this session's own writing: a personal GitHub username and a contributor's first name, both in committed docs. Fixed the content and the underlying gap that let it happen.

- `CHANGELOG.md`, `.dev/sessions/2026-07-08T194803.md`, `.dev/sessions/2026-06-19.md`: scrubbed the leaked username and name to generic placeholders ("the developer," "a personal fork"); the historical-immutability rule for prior-day session files doesn't cover personal-info exposure, treated the same as the existing "no credentials, ever" exception
- `CLAUDE.md`, `AGENTS.md` (repo root): added the "no machine-specific paths" constraint, which only existed in `template/` for downstream adopters, not here where the leak actually happened; broadened it in both places to explicitly cover usernames and real names, not just paths
- `template/CLAUDE.md`, `template/AGENTS.md`: broadened the existing constraint to match
- `CONTRIBUTING.md`: added a matching design principle and a pre-commit grep step (own OS username, git identity, personal fork names) alongside the existing agent-neutrality check
- `CHANGELOG.md`: logged `no-personal-identifying-details` under Unreleased

Live-tested the automatic upstream-check on a real pre-agentics project after gaining file access to it. It correctly did not fire: the project's own `CLAUDE.md` has a complete "Starting a session" checklist and a "Workflow" line that explicitly names which topics defer to global context, not including session discipline. This is the project's own instructions correctly taking priority over global dispatch, not a bug — confirms the first migration of an old, complete, non-deferring project genuinely can't be fully passive, only projects that are new or already migrated get that.

- `template/conventions/upgrading-adoption.md`: added this exact pattern (complete legacy checklist + a dispatch line that names topics but omits session discipline) as a named diagnostic signal in § 1, rather than leaving it as an unexplained gap the check might otherwise seem to have
- `CHANGELOG.md`: logged `upgrade-diagnosis-legacy-checklist` under Unreleased

Manually ran `conventions/upgrading-adoption.md` end to end on that same project (Arranger) to see what it would produce — this was a misstep: the point of the exercise was to confirm the *automatic* session-start path applies the migration on its own, and doing it by hand instead short-circuited that exact test. The developer reverted the Arranger-side changes afterward. The diagnostic gaps found while running it were real and are kept below; the Arranger changes themselves are not, and the automatic-path test is still open.

Two gaps surfaced only by actually doing the migration, not by reading the procedure: `CLAUDE.md` and `AGENTS.md` had quietly diverged from each other (six-step vs four-step "Starting a session" checklist, each individually plausible against the template), and a third file (`.github/copilot-instructions.md`) that the project's own `DEVELOPMENT.md` explicitly says to keep in sync wasn't covered by the procedure at all.

- `template/conventions/upgrading-adoption.md`: added "do CLAUDE.md and AGENTS.md actually agree with each other" and "other agent-specific instruction files kept in sync per the project's own docs" as explicit § 1 diagnosis items; also fixed § 2's "Apply" step to mention inlining into `CLAUDE.md` as well as `AGENTS.md`, matching what § 1 actually diagnoses
- `CHANGELOG.md`: logged `upgrade-diagnosis-cross-file-drift` under Unreleased

Given the actual automatic-path test on Arranger can't run right now (would collide with the developer's own concurrent work there, and re-testing it wouldn't show anything new anyway — its own legacy checklist is the blocker, not agentics), tested the mechanism properly instead: two throwaway fixtures in scratch, neither carrying a legacy competing checklist, run through blind subagents with no memory of this conversation and no filesystem writes permitted.

- Fixture 1 (tag present, `synced` pointing at a real older agentics commit): the agent correctly ran the real `git log`/`git diff` against the actual local agentics clone, found the four genuine commits since that point, led with the one marked `breaking: yes`, and asked before doing anything rather than bulk-applying. Steady-state path confirmed working end to end.
- Fixture 2 (mentions agentics, no tag at all): first run exposed a real regression — `convention-levels.md` and `upgrading-adoption.md` each pointed at the other for "the surfacing/catch-up treatment" and neither actually contained it anymore, lost when `convention-levels.md` was refactored to defer to the new file. The agent correctly identified there was nothing concrete to follow and asked rather than inventing something.
- `template/conventions/upgrading-adoption.md`, `template/conventions/convention-levels.md`: moved the concrete "surface current `## Unreleased` as a bounded catch-up" instruction into the one place it's actually reached (`upgrading-adoption.md` § 1's "Version tag" bullet); `convention-levels.md` now points at that specific location instead of the other file in general
- `CHANGELOG.md`: logged `restore-unreleased-catchup` under Unreleased
- Re-ran fixture 2 after the fix: correctly followed the chain to the real instruction, surfaced the two actual `breaking: yes` entries by name, and asked before proceeding — confirms the no-tag bootstrap path now works too

Redesigned the upstream-check on direction from the developer: it should be mandatory and persistent for agentics contributors specifically (a narrower group than "AI engineers" generally), not just an opt-in that can go quiet after one mention, with an explicit two-scope dismissal rather than inferring one from silence. Also corrected to stay agent-neutral throughout per explicit reminder — this isn't a Claude-only mechanism.

- `template/conventions/convention-levels.md`: § Checking for upstream updates now has two tiers (opt-in via `propagation_suggestions`, mandatory via `agentics_contributor`); contributor tier recurs every session per unresolved gap regardless of a project's own opt-in setting, stops only on explicit `agentics_upstream_check: no` at project or global scope
- `template/conventions/session-discipline.md`, `template/AGENTS.md`: checklist step updated to gate on either flag
- `CONTRIBUTING.md`: `agentics_contributor: yes` now documented as belonging in global context, not agentics' own project memory, since it must apply everywhere a contributor works, not just inside agentics; added the third (mandatory-check) behaviour it now enables
- `CLAUDE.md`, `AGENTS.md` (repo root): initialization blocks updated to record `agentics_contributor` globally, not in project memory
- `CHANGELOG.md`: logged `contributor-mandatory-upstream-check` under Unreleased (breaking: existing contributors need to re-record the flag in global context for the new tier to apply elsewhere)

Checked whether the developer's own setup actually satisfied the new global-context requirement: it didn't. `agentics_contributor: yes` had never been literally recorded anywhere, agentics-local or global, only the broader "propagation always on" shortcut existed informally. Fixed for real rather than just in agentics' own docs.

- `~/.claude/CLAUDE.md` (outside this repo): added the explicit `agentics_contributor: yes` flag and its mandatory-check implication to the developer's own initialization shortcuts
- Validated all three branches of the new tier design against a throwaway fixture (mandatory check firing with no project-level opt-in, global "stop for all projects," per-project "stop for this project") via blind read-only subagents: all three passed on the first run, no fixes needed this round
- Validated the remaining default case (no `propagation_suggestions`, no `agentics_contributor`, plain project): check correctly stays silent, with the right reasoning stated. Full six-scenario test matrix for the upstream-check mechanism now passes: steady-state diff, no-tag bootstrap, mandatory-despite-no-opt-in, global dismiss, per-project dismiss, and the plain default

The developer clarified the actual intent behind the mandatory tier: the check should run regardless of what a project's own files say, not merely "every session" — the exact scenario the real Arranger test already failed on. Rebuilt that blocking pattern faithfully in a throwaway fixture (complete local checklist, a Workflow line naming other topics but not session discipline, matching Arranger's actual `CLAUDE.md` almost verbatim) and tested a plain "mandatory" instruction against it first: failed, same as the real test, confirming the mandatory-tier work alone didn't close this gap. Rephrased as an explicit override — naming the failure mode directly and stating that neither a complete checklist nor a narrower dispatch line opts a project out — and it worked.

- `template/conventions/convention-levels.md`: added the override requirement as its own point in § Checking for upstream updates, with the tested wording pattern, since "mandatory" alone reads as a frequency instruction rather than a precedence one
- `~/.claude/CLAUDE.md` (outside this repo): added the same explicit override language to the developer's real global config, replacing the plain mandatory-flag description
- `CHANGELOG.md`: logged `contributor-check-explicit-override` under Unreleased

A separate, unrelated session in Arranger (roadmap work, closed and committed by the developer) surfaced its own propagation candidate: verifying against actual code/file state before marking a `.dev/` entry done or resolved held up well across a full session. Folded into this same batch rather than deferred, since it's small and fits the theme.

- `template/conventions/session-discipline.md`, `template/AGENTS.md`: added to § Keeping `.dev/` current: verify against current code/file state before writing a status change, not against a prior description or session summary
- `CHANGELOG.md`: logged `verify-before-writing-dev-docs` under Unreleased

The developer tested the override for real: told the same Arranger session to "start a new session" mid-thread (not a fresh chat instance) and it correctly treated that as a session-start signal, re-read `session-discipline.md` fresh rather than trusting its own memory of the file, noticed the file had changed substantially, and ran the real checklist against Arranger's actual state — finding no tag, correctly declining to diff, and correctly deferring to `upgrading-adoption.md` § 1 rather than guessing. Stopped and asked before changing anything, exactly as designed. Its own explanation of *why* the check ran conflated project-level detection with agent-level tier strength, though it still reached the right answer.

- `template/conventions/convention-levels.md`: reordered § Checking for upstream updates so detection (a project property) and tier strength (an agent property) are answered as two separate, ordered questions instead of blurring together, with an explicit line that a project mentioning agentics does not by itself make the check mandatory
- `CHANGELOG.md`: logged `separate-detection-from-tier` under Unreleased
- Nothing applied in Arranger yet — the developer is holding at the diagnosis/offer stage, which is exactly where the design says to stop until explicit consent

Audited the repo for a separate, unrelated concern: whether agent-facing files clearly rule out a human reader, since neither filename convention nor context alone reliably conveys that. None of the four primary entry points did.

- `CLAUDE.md`, `AGENTS.md` (root and template): added an explicit "for AI agents" line pointing a human reader to `README.md`/`CONTRIBUTING.md` instead
- `docs/agent-security.md`: marked as intentionally dual-audience (human threat-model reading, agent-actionable integrity check) rather than left ambiguous
- `CONTRIBUTING.md`: added "Explicit audience" as a design principle for future agent-facing files
- `CHANGELOG.md`: logged `explicit-agent-audience` under Unreleased

The developer closed and reopened the same Arranger session twice mid-thread to test this further, live. First reopen (before this session's edits landed) went straight to the diff with no re-read narration — correct, since nothing had actually changed yet, not a skipped step. Second reopen (after the `~/.claude/CLAUDE.md` override rewrite and the `convention-levels.md` reorder) narrated precisely: correctly distinguished which file changed from which didn't (explicitly confirmed `session-discipline.md` was a no-op re-read), quoted the new override language, mapped it to Arranger's actual "Workflow" line verbatim, and named its own shift from inferring applicability via role reasoning to it being explicit and unconditional under the new wording. Landed on the same correct diagnosis as before and stopped to ask again rather than proceeding. No further agentics changes needed from this: it's confirmation, not a new gap.

Logged a roadmap item rather than acting on it now: the same live testing was noticeably verbose and technical (file diffs, precedence mechanics) — appropriate for the dev/AI-eng/contributor audience actually running it, but flagged as likely overwhelming for a general (non-dev) user under the same convention. `CLAUDE.roles/*.md` currently only shape which conventions apply per role, not how verbose the agent's own responses should be; that's a separate, currently-unaddressed dispatch.

- `.dev/roadmap.md`: added "Response verbosity/tone calibration per role" under Design questions: needs more thinking, distinguished from the existing "Dev vs. non-dev users" item (which is about convention content, not response style)
