Backfilled retroactively: a long session covering the upgrade-mechanism redesign, several new code-style and interaction conventions, a real incident (bootstrap artifacts wrongly copied into two live projects) diagnosed and fixed across three repos, and a new contributor-only testing practice built and immediately proven against agentics' own files.

- `template/conventions/convention-levels.md`, `template/conventions/upgrading-adoption.md`: the upstream-check now runs the structural diagnosis directly whenever there's something to reconcile, instead of only surfacing changelog text or offering the full procedure; gaps split into a single batched non-conflicting confirmation versus individually-asked conflicts, with permanent conflict resolutions recorded in a new `.dev/agentics-overrides.md` (created on demand)
- `template/conventions/code-style.md`: added "Matching existing configuration entry points" (locate a project's existing config-resolution mechanism before adding a parallel one), "No non-null assertions" (never `variable!`, regardless of `tsconfig` strictness), "Verifying dated or versioned external facts" (check a versioned claim like an OWASP edition against its source at the point of use, not from training data), "Always brace control-flow blocks" (no brace-less single-line `if`s, for diff cleanliness); fixed two of the file's own pre-existing examples that had violated the new brace rule
- `template/conventions/security.md`, `template/conventions/security-guidelines.md`: OWASP references rephrased from a passive "always verify the current edition" into a check triggered at the point of naming an edition
- `CONTRIBUTING.md`: added a design principle that convention entries state the rule plus edge-case reasoning inline and cross-reference `CHANGELOG.md` by slug for incident narrative rather than re-narrating it; applied retroactively to `session-discipline.md`, `convention-levels.md`, `upgrading-adoption.md` where safe to separate from the instruction itself; added a simplification pass to the publish ritual; added dry-run-against-a-fixture and the consistency script as required steps before a dispatch-table or verbatim-copied-procedure change is considered done
- `AGENTS.md`, `template/AGENTS.md`: added "Sanity check requests" to Interaction parameters (review the whole conversation, not just the latest message, when asked to sanity-check an idea); root `AGENTS.md` backfilled a missing "don't self-edit instruction files" Critical constraint that `template/AGENTS.md` already had, found by the new consistency script's first run
- `template/README.md`, `template/conventions/upgrading-adoption.md`, both `AGENTS.md`: real incident, `conventions/`, `CLAUDE.roles/`, and `CLAUDE.softeng.md` got copied into two live adopting projects (an upgrade and a fresh bootstrap) because the dispatch table's bare relative paths were ambiguous once copied verbatim, and the "skip if global context covers it" wording read as optional rather than gated. Fixed: this content is global-guideline material, never copied into a project under any circumstance; missing locally is always correct, not a gap
- Cleaned the incident up directly in the two affected external projects: removed the wrongly-added `conventions/`, `CLAUDE.roles/`, `CLAUDE.softeng.md` from both, synced the corrected dispatch-table note into each project's own `AGENTS.md`
- New `testing/` directory at the repo root (contributor-only, never copied into an adopting project): `README.md`, `fixtures.md` (four disposable fixture shapes for dry runs), `regression-checklist.md` (curated past behavioral incidents restated as re-verifiable scenarios), `cold-read-review.md`, `consistency-checks.md` plus a real, executable `scripts/check-consistency.sh`
- `.dev/roadmap.md`: logged PR review workflow, the release-process convention (in progress elsewhere, not here), the "modality" interaction-mode framework (parked, not designed), a set of coding-convention proposals awaiting agreement, and a research item on software-based testing options for agentics, explicitly scoped as research, not a build commitment
- Two `CHANGELOG.md` entries dated 2026-07-17 (written after real time had actually moved to 2026-07-21, a stale date carried forward instead of re-checked) corrected to the real date; this file itself created to close the gap of no session log existing for today at all despite the volume of work above
- Checking Lyric and Maestro's `.dev/` files for the bootstrap-artifacts incident surfaced content problems the diagnosis never looked for: two individuals named directly in Lyric's `roadmap.md`, and a tech-debt-format instructional comment duplicated into both projects' `tech-debt.md`, restating what `session-discipline.md` already defines. `session-discipline.md` § Keeping `.dev/` current now states `roadmap.md`/`tech-debt.md` hold concrete decisions, not process or event narrative, tying this directly to where names sneak in; § Tech-debt entry format says not to restate itself in a project's own copy. `upgrading-adoption.md` § 1 now scans existing `.dev/` entries for exactly these three problems, not just file shape; § 2 treats fixing them as non-conflicting
- Fixed directly in both external projects: scrubbed the two names from Lyric's `roadmap.md`, removed the duplicated format comment from both projects' `tech-debt.md`
- Full-repo duplication/ambiguity audit (three parallel reviewers plus a direct pass), prompted by how much got introduced fixing the incidents above under time pressure without circling back to consolidate. Real findings fixed: the bootstrap-artifacts rule restated in full three times (`template/README.md`, `AGENTS.md`'s dispatch note, `upgrading-adoption.md` § 1), now consolidated into `convention-levels.md` § How much to keep locally with the other three pointing at it; the upgrade-mechanism classification scheme duplicated verbatim between `convention-levels.md` and `upgrading-adoption.md` § 2, now stated once; root `README.md`'s three contradictory positions on `CLAUDE.softeng.md` plus a stale CLAUDE.md/AGENTS.md tag-location claim; `CONTRIBUTING.md` restating `AGENTS.md`'s Critical constraints instead of citing them, and its manual grep steps never reconciled with the new consistency script; root/template `AGENTS.md` drift (refined Interaction-parameters wording never back-ported, "Name code, not people" missing from root, the grep-before-committing step missing from template); `security.md`/`security-guidelines.md`'s duplicated Quick-threat-model block and an ambiguous relative path; two near-identical `security-guidelines.md` copies with no documented sync relationship, now declared and actually synced; an unnamed convention reference in `documentation.md`; and `testing/`'s own files violating the terseness principle they were built alongside, incident narrative retold in three files instead of cross-referenced once, plus a stale step-number reference, an undocumented scope restriction in the consistency script, and underspecified fixture-running instructions. All fixed; script re-run afterward to confirm, all green, including the root/template `AGENTS.md` parity it now reports
- `AGENTS.md` § Repo maintenance rules: "agentics is adopter zero of its own template", considered and rejected collapsing root `AGENTS.md` into a bare pointer at `template/AGENTS.md` (root has real, necessary content that only makes sense for agentics itself, the same shape any adopter's lean `AGENTS.md` should have), and instead wired agentics to run its own `upgrading-adoption.md` procedure against itself whenever `template/AGENTS.md`/`template/CLAUDE.md` changes. `upgrading-adoption.md` names this as a third way in; `CONTRIBUTING.md`'s fixture-dry-run step references it explicitly. Run for the first time immediately: confirmed clean by both the mechanical section-count check and an actual content-level read-through
- New `docs/deterministic-by-design.md`, dual-audience (agentics contributors, anyone building a similar system): a three-way triage for reliability, mechanical checks become scripts wired into something that always runs, judgment calls that keep getting skipped need an explicit override naming the specific excuses that don't count, and some ambiguity classes should be made structurally impossible rather than caught after the fact. Linked from `README.md` § Contributing. Applied immediately, not left as theory: `.githooks/pre-commit` runs `check-consistency.sh` and blocks the commit on failure (`CONTRIBUTING.md` documents the one-time `git config core.hooksPath .githooks` setup); `CONTRIBUTING.md`'s fixture-dry-run/adopter-zero step now states explicitly that a change looking small or already reviewed once isn't an exception; `check-consistency.sh` gained a narrow regression guard for the specific "live pointer, not local copy" sentence that closed today's bootstrap-artifacts incident
- Both `AGENTS.md`'s "surface better alternatives... not only after being asked" now explicitly covers checking whether a just-shipped fix still depends on being remembered, unprompted, rather than waiting for a broader question to surface it, the exact gap that took a direct "how do we make this bulletproof" question to close today
- Committed and pushed all of the above to both remotes (`origin`, `softeng-fork`); `.githooks/pre-commit` fired for the first time on a real commit and passed. Push initially failed on a local credential-helper issue (git's `osxkeychain` helper couldn't reach Keychain from this shell); fixed with `gh auth setup-git`, then both pushes succeeded
- Developer found individuals named directly in this repo's own `CHANGELOG.md` (ironically, inside the entry describing the earlier name-leak fix, which quoted the leaked text verbatim) and in an older, closed session file, both already pushed to both remotes. The same leak was independently found in Lyric (a line the earlier partial fix missed) and in `overture-infra` (undiscovered until now, also already pushed). Scrubbed all four; logged as `scrub-names-missed-by-own-fix`. Not fixed: the names remain in the old commits' history on all three affected remotes unless that history is separately rewritten
- Developer flagged a real session-file line as exactly what "do not log conversational activity" already prohibits. Left the historical line alone, a quality problem, not a Critical Constraint violation, and not what the immutability exception is for; added it (anonymized) as `session-discipline.md`'s first worked example of the rule instead
- Session reopened: developer caught that the `deterministic-by-design` fix to "surface better alternatives" was itself too narrow, it stated one specific instance (checking whether a fix still depends on being remembered) as if it were the general rule, missing the actual broader principle: offer ideas and improvements unprompted whenever already visible, not only once asked an open-ended question, of which that check and "surface an alternative before implementing" are both just examples. Reworded both `AGENTS.md`s to lead with the general principle; since Lyric and Maestro had already synced `0.6.0` and picked up the narrow wording, fixed both directly rather than waiting for their next upgrade check. Session reopened again: developer flagged the corrected bullet as still overly verbose next to the rest of Interaction parameters, sessions and CHANGELOG entries excluded, those are fine as narrative. Trimmed to one line stating the principle, one listing the two cases as bare examples, and a pointer to `CHANGELOG.md` instead of re-explaining the incident inline. Fixed in both `AGENTS.md`s plus Lyric and Maestro's already-synced copies. Generalized into a standing design principle: `CONTRIBUTING.md` now names succinct wording as a separate pass from non-duplication, since the same bullet needed trimming twice in a row for two different reasons. Session closed for the night here
