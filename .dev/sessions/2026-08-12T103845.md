Real, external team feedback (Slack, not a hypothetical) turned into two convention fixes: an agent had created a symlink on a developer's own machine to work around a Docker-socket path mismatch, without disclosing it, and the resulting fix wasn't reproducible when a teammate applied the same change elsewhere.

- `AGENTS.md`'s "Check in before non-trivial decisions" (root and template) generalized to name the developer's own machine and OS-level state explicitly (installed packages, symlinks, sockets), not just global config files, as needing the same check-in as any other lasting footprint outside the current project. Connected explicitly to a separate piece of the same team feedback: undisclosed-but-otherwise-reasonable actions are exactly what compounds into general distrust of an agent's permission behavior, invisibility is the actual problem, not just reproducibility.
- `definition-of-done.md`'s checklist gained a new item, reproducibility: a fix isn't done if it only works because of an undocumented, machine-specific accommodation, either generalize it or capture it somewhere reproducible. Cross-platform divergence named explicitly as a common instance, softeng's own work isn't Mac-only by default.
- `code-style.md` gained "Platform portability": default to detecting the runtime or making it configurable, don't hardcode what one machine happens to need.
- `CHANGELOG.md` and `testing/regression-checklist.md` entries added (`undisclosed-machine-state-change`); dash-check and full consistency script clean.

Separately, earlier: fixed a real multi-session gap in the upstream-check procedure itself, caught live during agent-index testing. `convention-levels.md` § Checking for upstream updates' empty-diff-means-stop logic couldn't distinguish "I already checked this" from "a different, concurrently active session already advanced the shared `synced` marker." Added a caveat: a session's own first check this conversation still skims current content even on an empty diff. `upgrading-adoption.md`'s "update your agentics" trigger now runs unconditionally, an explicit ask always verifies for real. Independently re-confirmed by a second, different session in a different project, verified directly against that project's file, not accepted on the report alone. `CHANGELOG.md` and regression-checklist entries added (`upstream-check-empty-diff-not-personal-catchup`).

A long real conversation with a prospective adopter's own feedback (relayed via Slack), turned into a substantial `README.md` rewrite plus one new mechanism and one new writing rule.

- `README.md`: replaced the opening tagline (it overclaimed an instant "your agent knows how to behave") with a concrete "what this is / does / lets you do" structure; split Quick Start into two genuinely separate entry points, personal/global-only versus project-wide/team, rather than one flow that always ends in repo-committed files; defined "accessible to your agent" operationally where it's first used, with a forward link instead of an undefined precondition; stated the required session context (working directory must be the target project) explicitly for the first time; synced the listed setup-question count (4 → 6) to what `template/AGENTS.md`'s initialization actually asks; added a git-status verification checklist independent of trusting the agent's own narration of what it did; added a real example entry from this repo's own `tech-debt.md` next to the file listing, showing concretely what committed devctx content looks like; and stated plainly that the payoff comes from sustained use, not installation.
- New mechanism: `global-context/personal-preferences.md` plus a "Bootstrapping without a project" procedure in `global-context/README.md`, since the "personal, global-only" path needed an actual mechanism behind it, not just new wording, universal Interaction parameters and Critical constraints, bundled for a developer who wants agentics' behavioral conventions without adopting it into any project. Caught before shipping: the file was initially named `CLAUDE.md`, inconsistent with every sibling file in that directory; renamed to match the existing agent-neutral naming pattern.
- New writing rule, `session-discipline.md`: "Describe the system, not the struggle to find it," the sharper cause behind the adoption hesitation wasn't procedural, developers can feel exposed committing `.dev/` content that reads as a record of their own mistakes. Added the distinction between a negative result worth keeping (a system fact) and the struggle to reach it (a personal account, strip it), with a before/after example modeling the transformation rather than stating a rule about confidence or safety; deliberately no mention of exposure or psychology in the convention text itself, that reasoning lives in the `CHANGELOG.md` entry instead. `documentation.md` § The atlas cross-references it.
- `CHANGELOG.md` entries added for all three (`onboarding-doc-underspecified-preconditions`, `personal-global-only-bootstrap`, `struggle-narrative-vs-system-fact`); regression-checklist entry added for the one genuinely behavioral fix (the writing rule); the other two are documentation/new-capability additions, correctly excluded per that file's own stated scope.
- Ran the full publish-readiness sequence: staleness pass on `.dev/tech-debt.md` (unrelated, clean), CHANGELOG-vs-regression-checklist audit (clean), full consistency script (clean). Published as 0.14.0, pushed to `origin` only (personal fork), per explicit scope this time; `softeng-fork` pushed separately, by hand.
- Real question caught right after publishing: does a personal/global-only setup ever create project-level devctx scaffolding in a project that hasn't separately adopted agentics? True by construction already (`personal-preferences.md` has no dispatch table or initialization content), but never stated as an actual guarantee. Added an explicit section stating it directly. `CHANGELOG.md` entry added (`personal-preferences-no-project-scaffolding`, patch); no regression-checklist entry, this is a proactive clarity fix, not a fix for an observed misbehavior.
- Real design insight: agentics is fundamentally a personal, global thing; project adoption adds persistent memory and project-specific overrides on top of that, it doesn't duplicate the global foundation from scratch, and shouldn't ever end up *ahead* of it. Two concrete fixes followed. `global-context/personal-preferences.md` gained its own version tag (stamped on bootstrap, same pattern as project `AGENTS.md`); `convention-levels.md` § Checking for upstream updates now runs the same diff-and-diagnose procedure against it independently, and treats a project's tag being ahead of the developer's own global one as an anomaly to fix immediately, not just note, since refreshing the global tag is cheap and personal, unlike a project update. `README.md`'s Quick Start reworded from two parallel, equally-weighted choices into an explicit sequence (global first, project-level second, additive), with the redundancy in step 2 (a full `AGENTS.md`, not just a pointer back to global) stated as intentional: a teammate without their own global bootstrap still needs consistent behavior in that repo.
- `CHANGELOG.md` entries added (`global-tag-version-tracking`, minor; `quick-start-sequential-not-additive`, patch); regression-checklist entry added for the behavioral one (`global-tag-version-tracking`), the Quick Start reframing is documentation-only, correctly excluded. Dash-check and full consistency script clean throughout. Still sitting in `## Unreleased changes`, not yet published.
