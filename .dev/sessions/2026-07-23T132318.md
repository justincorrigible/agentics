Queued a new convention for the next release: agents were adding AI-tool attribution trailers to commits and PRs, which reads as the project endorsing a specific commercial vendor.

- `template/conventions/code-style.md` § Git: added "No AI-tool attribution in commits or PRs", no "Co-Authored-By"/"Generated with"-style trailers naming an AI tool or vendor in commit messages, PR descriptions, or PR comments, regardless of which agent is doing the work
- Applied the rule retroactively: two already-published commits (`c23a8c5`, and the `0.6.1` release commit `dfb0b38`) carried the trailer. Stashed all uncommitted work, rewrote both non-interactively via `git filter-branch --msg-filter` (macOS `sed` doesn't support the GNU multi-line cleanup syntax; a plain line-delete filter worked), cleaned up the backup refs and ran `gc`, updated both local `main` and `softeng/main` to the rewritten history, then restored the stashed work. Deliberately left unpushed: this stays local until the next release folds it in, so the `synced:` SHA every downstream repo currently references stays valid on the remote until then
- `upgrading-adoption.md`, `convention-levels.md`: this exact situation, a `synced:` SHA ceasing to resolve after a history rewrite, could recur for other reasons; added § Troubleshooting an unresolvable `synced` SHA distinguishing "empty diff" from "diff errors outright," with the latter treated like a missing tag rather than left unhandled
- Checked whether any other troubleshooting-shaped content elsewhere in the adoption/upgrade flow belonged consolidated into the new section. Two candidates considered and left in place: `convention-levels.md`'s stale-local-clone fallback (core branching logic of locating the template, not a rare/surprising breakdown) and `upgrading-adoption.md` § 1's note on why the automatic check silently never fires for a pre-agentics `CLAUDE.md` (a normal diagnosis finding, not the mechanism itself erroring)
- Published `0.10.0`: relocated all five `Unreleased` entries under the new heading, committed locally without the AI-attribution trailer (dogfooding today's own new rule). Not yet pushed: the rewritten history (see above) still needs a force-push to both remotes, held back per explicit instruction
