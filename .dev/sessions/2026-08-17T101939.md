Fixed a documentation-drift gap surfaced by real feedback from an imicroseq/portal-ui agent reviewing a project's docs for completeness.

- Root cause: `session-discipline.md`'s "Refinement passes" checkpoints are explicitly diff-scoped, reading `git diff` against each touched file, which catches prose quality but never duplication, a broken cross-reference to a since-removed section, or content landing out of order relative to the rest of the document, since each contributing edit can be locally fine on its own and none of those defects show up in any single diff.
- Corrective fix: at the "before committing" and "before ending a session" checkpoints, a doc with two or more edits in the same stretch now gets read whole, not diffed, checking cross-reference validity, duplication, and ordering.
- Preventive fix: `documentation.md` § Cross-linking, not duplication now says to search for existing coverage of a topic before writing a new section for it, the same "Searching before writing" habit `code-style.md` already requires for code.
- `CHANGELOG.md` and `testing/regression-checklist.md` entries added (`whole-document-coherence-pass`). Dash-check and full consistency script clean. Sitting in `## Unreleased changes`, not yet published.
- Removed the Members `name`/ref field from the just-shipped agent-index convention entirely, after real feedback across three separate incidents (a from-scratch adopter unable to self-derive its own name at registration; a lookup finding a registered name matched no active session at all) made clear the field itself was the problem, not just first-registration. Reaching a Member now resolves live: filter `ListAgents` by `project`/`window`/`scope`, default to the most recently started candidate, fall back to a Requests post or the developer if that's wrong. "Validating a callback" collapsed to one plausibility-based rule instead of a separate exact tier, since that tier depended on the removed field. Also added: propose `label`/`project`/`window`/`scope` from visible context and confirm with the developer before committing an entry. Updated the bootstrap template and the live `~/.claude/agent-index.md` (schema and all existing Members entries) to match. `CHANGELOG.md` and regression-checklist entries written directly against this final design, not chronicling the intermediate `name: pending` step, which never actually shipped.
- Closed the loop with the session that raised this feedback (iMicroSeq Gateway and Pedigree, self-named "Portal Gateway + Pedigree" on reply): found by filtering `ListAgents` for the most recently started candidate rather than any stored name, exactly the new procedure being confirmed. Positive data point, not just the failure cases: their own direct attempt to reach agentics by name had gone to the wrong session, and the Requests-board post is what actually connected the two sides, the layered fallback working as designed in a real case. Corrected one misattribution in their reply: they credited a different session ("all sorts of Helm Charts") with having already updated the live index file; that was this session, done directly earlier tonight.
- Read every file in the repo start to end (full pass, not incident-driven) and wrote up `.dev/docs/atlas/whole-repo-review-2026-08-17.md`: a correction (four files, `CLAUDE.softeng.md` and three `CLAUDE.roles/*.md` files, still reference `CLAUDE.md` as canonical instead of `AGENTS.md`, `CLAUDE.overture.md` gets it right and served as the working comparison), two clear-cut gaps (`docs/agent-security.md` dispatched from `session-discipline.md` for one narrow sub-case only, most of its actual content, indirect prompt injection, PR/CI injection, MCP poisoning, supply-chain risk, has no dispatch trigger anywhere; the credential-blocklist hook in `template/.claude/settings.json` is missing patterns this team's own documented stack needs, `kubeconfig`, `*.tfstate`, `.docker/config.json`, plus `.netrc`/`.pgpass` generally), and a bigger naming question flagged for a deliberate decision rather than an unprompted fix (`CLAUDE.roles/`/`CLAUDE.softeng.md`/`CLAUDE.overture.md` carry a Claude-specific-looking prefix despite being reached entirely through `AGENTS.md`'s agent-neutral dispatch, cutting against the agent-neutral design principle; renaming is real, coordinated, breaking work, not something to do unprompted). Verified the OWASP 2025 edition is still current directly against owasp.org rather than assuming. Fixed one small, self-caused gap directly while reviewing: `template/global-context/README.md`'s own file list never got `agent-index.md` added when that convention shipped earlier tonight. `.dev/docs/atlas/index.md` and `.dev/roadmap.md` updated with pointers. Dash-check and full consistency script clean.
- Applied the three clear-cut fixes from that review by direct instruction: the four "CLAUDE.md" → "AGENTS.md" corrections; `docs/agent-security.md` added to `template/AGENTS.md`'s "Security-relevant work" line (template only, root has no such line, correcting the review's own imprecise "both root and template" framing); the credential-blocklist hook's seven new patterns (`kubeconfig`, `.tfstate`, `.docker/config.json`, `.netrc`, `.pgpass`) added programmatically after a hand-edit broke the file's JSON validity, then functionally tested against sample paths, not just assumed correct.
- Investigated the developer's specific case: Arranger's `.github/copilot-instructions.md`. Not gitignored, tracked, correctly placed, contrary to the original suspicion. Read it side by side with `AGENTS.md` instead: it's a full, independently hand-maintained copy with no version tag or agentics pointer at all, and it's already drifted, missing the entire agentics upstream-check session-start step and several security-trigger details `AGENTS.md` has gained since. Combined with Copilot now reading `AGENTS.md` directly, the real fix isn't "keep it in sync," it's "retire it": updated `upgrading-adoption.md`'s diagnosis bullet and `CONTRIBUTING.md`'s design principle to recommend retiring or stubbing a Copilot-specific instructions file rather than syncing it. `CHANGELOG.md` and a regression-checklist entry added (`copilot-instructions-retire-not-sync`). Arranger's own file itself not touched, that's the developer's call on a real, different project. Dash-check and consistency script clean.
Checked the one external fact still open on the copilot-instructions item before touching it: GitHub Copilot's coding agent, its CLI, and VS Code's Copilot Chat (default on) all now read `AGENTS.md` directly, changing that item's actual shape from "help projects create `.github/copilot-instructions.md`" toward "confirm `AGENTS.md` already covers Copilot." Left that item and the rest of the copilot-instructions/verbatim-reconciliation work queued, still owed a developer answer on scope.
Finished the two items with no open questions. Commit-message register: added the two real approved messages verbatim to `session-discipline.md`, plus a constructed contrast showing the same change written in the session file's own fuller register instead, naming the actual trigger (carrying that register over, not the work's length or difficulty). Agentics-tag conflation: added an explicit warning to `convention-levels.md` right before the tag-comparison steps, agentics' own current version and a project's `synced` tag answer different questions, grounded in the actual helm-charts incident. `CHANGELOG.md` and one regression-checklist entry (the tag-conflation fix; the commit-message one is a content addition to an existing rule, not a fresh regression scenario). Dash-check and consistency script clean.
Shipped all three of Arranger's confirmed findings from earlier tonight. Full-suite verification: `session-discipline.md`'s "Refinement passes" semiregular in-session checkpoint now runs the whole suite, not just new tests, specifically because that's the checkpoint an involuntary session end can still reach when "before ending a session" can't; cross-referenced from `testing.md` rather than duplicated. Reproduce, don't theorize: `review-conduct.md`'s "Ground truth over claims" gained the test-discrepancy instance alongside its existing live-system-diagnosis one. Naming conventions: resolved the whole bundled, previously-gated colleague proposal in `.dev/roadmap.md` at once (no `Type` suffix/`I` prefix, explicit return types, full-word identifiers), since Arranger's own finding independently validated the identifier-verbosity piece; the fc/zod-style package-alias nuance (a package's own short alias no longer gets an automatic pass, only genuine language/framework idioms do) came from reading Arranger's finding closely, not assumed. Roadmap items (both the bundle and item 47) pruned. `CHANGELOG.md` and regression-checklist entries added for all three. Dash-check and consistency script clean.
Closed roadmap item 75 for real: `definition-of-done.md`'s lessons-learned criterion already covered five of the six design elements resolved back on 2026-07-30, missing only what happens to a fact that doesn't clear the durable-knowledge bar alone. Added: the same "Refinement passes" checkpoints already enforcing the live check also ask whether several already-recorded memory entries, none individually clearing the bar, now accumulate into something worth promoting together, consolidating the redundant entries once promoted. Verified all six original elements plus both open questions are now covered before pruning the roadmap item itself, not just assumed. `CHANGELOG.md` and regression-checklist entries added (`lessons-learned-accumulation-promotion`). Dash-check and consistency script clean.
Addressed two queued design items using tonight's own work as the confirming evidence for both. `session-discipline.md`'s "Re-check before committing" only ever re-verified entries added the same session; generalized it to prune any `.dev/roadmap.md`/`.dev/tech-debt.md` item a change fully resolves, however old, cross-referencing `CONTRIBUTING.md` step 8 rather than restating it, the item-75 miss and tonight's own whole-repo-review roadmap prune both fed this. `review-conduct.md`'s "Being shown a mistake is itself a lessons-learned trigger" asked whether a mistake generalizes but never required checking; extended it with a concrete trigger (sweep every file of the same structural class before finishing) and boundary (structural patterns only, not one-off typos), grounded in three real instances from tonight (the definition-of-done self-trigger sweep, the CLAUDE.md role-file sweep, the 19-citation functional-value pass). Trimmed the matching roadmap bullet to what's still genuinely open, cross-project sweep reach. CHANGELOG and regression-checklist entries added for both (`prune-on-ship-generalized-to-devctx`, `structural-pattern-sweep`). Dash-check and consistency script clean.
Wrote up the two identity/reachability findings still owed from earlier tonight, grouped into one entry in `interagent-communication-findings.md` at the developer's suggestion (same underlying theme, opposite directions): the lasagna episode (two sessions, conflicting identity claims, the more-evidenced one turned out to be a knowledgeable bystander, not the actual requester, only an out-of-band word settled it) and the helm-charts reach-out (a peer hit a stale socket replying via the documented `from`-attribute pattern, then correctly refused to guess among three `agentics`-named `ListAgents` candidates, arriving independently at the same discipline the shipped convention now states as its own rule). Sharper lesson than the shipped rule captures on its own: evidence quality confirms a claim's content, not whose claim it is, when several sessions share legitimate access to the same devctx. Dash-check and consistency script clean.
Then, by further direct instruction, resolved the naming question the review had deliberately left open: renamed `template/CLAUDE.roles/` → `template/AGENTS.roles/`, `template/CLAUDE.softeng.md` → `template/AGENTS.softeng.md`, `template/CLAUDE.overture.md` → `template/AGENTS.overture.md`. Root/template `CLAUDE.md` untouched, they still genuinely earn the name. Found every reference by grep (11 live files plus the two role files' own self-references) and updated all of them; left historical, already-released `CHANGELOG.md` entries and old `.dev/sessions/` logs alone, a record of what was true at the time. `whole-repo-review-2026-08-17.md` and `.dev/roadmap.md` updated to reflect the resolution, the roadmap bullet removed entirely now that all four sub-items are done. `breaking: yes` CHANGELOG entry, with an honest migration note: an already-bootstrapped global context under the old filenames won't detect the rename automatically, nothing currently checks addendum-file staleness the way `personal-preferences.md`'s own version tag does. Dash-check and full consistency script clean throughout.
Ran a second review pass over the whole repo, six concurrent lenses (coherence, cold-read adoption, enforceability, coverage gaps, scale, security) instead of the morning's single linear read, with every reported finding re-verified directly before being recorded rather than accepted on a lens's report. That method difference is itself the first finding: nothing changed in the files between the two passes, and the second found materially more, including one defect a reading pass structurally could not catch. Written up as `.dev/docs/atlas/multi-lens-review-2026-08-17.md`, indexed in the atlas, with concrete defects logged to `.dev/tech-debt.md` and design questions to `.dev/roadmap.md`.
- Highest-severity finding, verified by executing the shipped hook against real payloads rather than reading it: the credential blocklist in `template/.claude/settings.json` has never fired. It reads `filePath`/`path`; Claude Code sends `file_path`, so the extracted path is always empty, no pattern can match, and every credential file returns `allow`. Present in the live global installation too, not just the template. The morning pass added seven patterns to this same file and reported them functionally tested; that test fed the pattern list directly and never exercised the payload contract, so it validated the half that already worked and passed against a hook that cannot fire. Not fixed: awaiting the developer's go-ahead, since one of the two affected files is their own global config.
- Second security finding: `review-conduct.md` grants a PR comment authored under the developer's recognized identity the same authority as a direct instruction, while `docs/agent-security.md` documents that exact channel as a 2026 CVE class demonstrated against Claude Code. Neither file references the other and the PR-review dispatch row omits the security doc, so the caution is never in context when the exception applies. The safe distinction (the platform's structured author field on a directly fetched comment is not attacker-controlled; attribution inside a PR body is) is available but unstated.
- Structural defect with the widest blast radius: an adopting project's dispatch table paths cannot resolve. `template/AGENTS.md` calls them live pointers into agentics then writes them bare; root `AGENTS.md` writes the correct `template/` prefix, so the prefix is dropped precisely in the copy that leaves the repo, no base is recorded anywhere during adoption, and resolving against the "Adapted from" URL 404s. `global-guideline-material-never-in-project` diagnosed this exact ambiguity and fixed only the symptom.
- Confirmed private-global-config leakage into shipped template files, twice, and diagnosed as one gap rather than two: outward propagation (project to template) has a full discipline, inward (a maintainer's private global config to template) has none, and that direction carries content already phrased as a general rule by someone who shares all the missing context, so it passes the placement question without being asked.
- Measured the growth trajectory rather than estimating it: session start now costs about 111,000 bytes of template text before any project file, `session-discipline.md` is up 123% across five releases, and `.dev/roadmap.md` is 91.5% one undifferentiated section. Agentics defined `roadmap_split: yes` for exactly this and exempted itself. Noted honestly in the write-up: documenting these findings grew the roadmap by a further 31%, which is the compounding loop the finding describes, demonstrated live.
- Coverage gaps confirmed by grep and checked against the existing roadmap so none is a restatement: the destructive-action gate exists once in the whole template, in the non-technical role file, while `dev.md` adds nothing, so the role holding a kubeconfig has no gate at all; debugging has no convention and no trigger despite three debugging lessons already existing, each filed where a debugging session never loads it; "say it is unknown rather than produce a plausible value" has been fixed three times narrowly and never stated generally, in a repo where zero files contain the words hallucination or fabrication; and nothing in the framework is addressed to the human, so every lesson ships as an agent-side rule with no developer-side counterpart.
Fixed the critical findings from the multi-lens review, prioritizing what costs adopters tokens on every session.
- Credential blocklist: rewrote the payload extraction to read `file_path` first (with `filePath`, `notebook_path`, `path` as fallbacks) and to tokenize `command` so a Bash `cat`, `cp`, or piped `grep` of a blocked path is denied too. Applied to the template and to the live global installation, which carried the identical defect; the live file was backed up first. Verified against 15 payloads covering every tool shape on both the deny and allow sides, rather than against the pattern list, since testing the pattern list is exactly what let the bug ship. `check-consistency.sh` gained a payload-contract check, and that check was negative-tested by reintroducing the original bug and confirming it fails; the first attempt at that negative test silently mutated nothing and proved nothing, which was caught and redone with an asserted mutation.
- `review-conduct.md`'s recognized-identity exception now attaches only to the platform's structured author field on a directly fetched comment, never to attribution appearing inside body text, and explicitly never converts into authorization for a destructive or externally visible action. The PR-review dispatch row now loads `docs/agent-security.md` for changes originating outside the team, so the threat model is in context when the exception applies.
- Made the adopter dispatch paths resolvable: `template/AGENTS.md` now states the base explicitly, gives a two-step resolution order, and names the two `docs/` paths as the repo-root exception. Root `AGENTS.md`'s three bare `conventions/` prose references, its false table preamble, and its row pointing at a seven-line stub all corrected.
- Removed both instances of private global configuration that had leaked into shipped files: repointed the dangling "Bulk text replacements" citation to `writing-style.md` § Dashes, and moved the Terraform and VSO ordering rule out of universal `writing-style.md` into `AGENTS.softeng.md`, phrased as the softeng reading of the general named-entry rule rather than a second copy.
- Split `conventions/upstream-check.md` out of `convention-levels.md`, which was 56% one section read on a different trigger than the rest of the file. Every live cross-reference repointed and verified by grep; historical changelog and session-log references deliberately left alone. Shipped as non-breaking because `convention-levels.md` retains a forwarding pointer, so an adopter with a stale dispatch table still resolves.
- Corrected the OWASP category (supply chain is A03 in the 2025 edition, not A08) in the file dispatched first for security work, the stale pre-refactor `CLAUDE.md` architecture claims in `convention-levels.md`, `CONTRIBUTING.md`, and `README.md`, the missing `AGENTS.overture.md` in the never-copy exemption list, a misattributed changelog slug, `testing.md`'s narrowed full-suite restatement, and three role-table cells left holding `|: |` by an earlier em-dash sweep.
- Added `conventions/context-economy.md` at the developer's request: the decision procedure behind this token work, written to be executed step by step by a less capable model rather than re-derived. Cost is bytes times read frequency; classify sections by trigger and split where a section's trigger differs from its file's; relocate rather than delete; leave a forwarding entry point; report both the section-only and whole-file reading models rather than only the favourable one.
- Honest measurement of the result, both models: reading only cited sections, the session-start set moved 95,849 to 98,269 bytes, up 2,420, entirely from the path-resolution block added as a correctness fix; reading whole cited files, which is what the dispatch actually names, it moved 107,202 to 98,269, down 8,933. The larger structural reductions (`session-discipline.md` at 52KB, the changelog archive) are logged in `.dev/roadmap.md` rather than attempted here, since splitting safety-critical git and commit rules out of an always-read file needs a mechanical backstop that does not exist yet.
Filled the security half of the human-facing gap the review found (nothing in agentics addresses the developer, so every lesson ships as an agent-side rule with no counterpart for the person). Added `docs/security-for-developers.md`, written for people rather than dual-audience, plus Security sections in the root and template READMEs, since both security documents were previously unreachable from either README and only `deterministic-by-design.md` was linked. Grounded the whole document in the inert-blocklist incident rather than abstractions: the three reasons it went unnoticed (a permissive failure produces no error to notice, the documentation asserted it worked, and it had been tested by a test that exercised the half that already worked) generalize past the bug, and the developer-facing takeaway is phrased as a question to ask rather than a principle to hold. Covers the blocklist's real limits, a table of agent claims that are unreliable specifically because they are claims about the agent's own behaviour, the trust boundaries only a person can hold, committed prose as a leak path with an honest account of how narrow the mechanical check really is, how to review an agent-proposed security change, and what to do once something has already gone wrong. The verification command published in it was run verbatim against both the template and the live installation before shipping, rather than written from memory, since publishing an unverified check would repeat the exact failure the document is about.
Cross-session exchange with an adopting project's session produced two convention changes this session could not have derived on its own, both from the recipient reporting back on how a correction landed rather than on whether it was right.
- Confidence bundling: a correction sent to that project mixed a checkable claim about the convention's text with an unverifiable prescription about that project's own data ("expect most entries to need extracting"), delivered at one confidence level. The prescription was wrong, 47 of its 73 entries already conformed, and acting on it as delivered would have produced exactly the fragmented-reasoning outcome the convention exists to prevent while appearing to comply. The recipient avoided it only because its own conventions require verifying a peer claim; nothing in the message marked one half as softer. `review-conduct.md` now requires separating what was verified from what is assumed about the other side's situation, on the stated principle that you can know a convention's text and cannot know their repo.
- Delivery shape, not just accuracy: an absolute claim ("that phrase is not in the convention") leaves a recipient two moves, concede or hunt a counterexample, which is adversarial framing for a collaborative task; naming the evidence instead ("I grepped these two files") invites them to supply the missing piece and would have reached the second location faster. Recorded as raising the weight the scoped-search rule deserves, since it fixes the exchange and not only the claim.
- Reframed `scoped-search-unscoped-claim` on the recipient's warning that a convention read as one team's discipline problem gets underused. It cited two instances, both from this repo; broadened to six across two independent sessions in one day, including three volunteered by that project with no mechanism in common (a shell glob's no-match abort read as absence, a test harness gating on the wrong property so the path never executed, a symbol reachable only via a wildcard re-export), and named the unifying property rather than the culprits: each produced a false negative that read as a clean result, so the mitigation has to be procedural.
- Ownership: this session had tracked that project's in-flight migration decision as an item pending with the developer and offered to drive it, when the work was theirs and already being exercised, and had presented it as open without checking it still was. `AGENTS.md` § Interaction parameters (root and template) now states that another session's work is not yours to pick up, finish, or decide unless explicitly asked, with the carve-out stated in the same breath because it is the valuable half: a peer's report revealing a gap in your own scope is yours to act on immediately. Their migration has since landed complete and conformant.
Closed the last standing queue item, which grounding split into three parts with two different answers than the item assumed.
- Generalized `upgrading-adoption.md`'s verbatim rule from the dispatch table to any non-conflicting content applied during reconciliation, since the failure it prevents (paraphrase silently dropping qualifying clauses, invisible because the result reads fine) is not table-specific. Added the carve-out so it does not read as absolute: content recorded in `.dev/agentics-overrides.md` keeps its local wording and is not applied at all, a different case from paraphrasing.
- Dropped the companion proposal to record a per-upgrade `verbatim-copy | changelog-paraphrase` marker in project memory. It is a write enabling an audit nobody has run, in a store that is per-agent and per-machine so it under-counts silently while reading as complete, the same reasoning that rejected a private provenance tally earlier the same day.
- The renamed-bootstrap-file gap turned out to be two problems, not one. Project-side staleness (an adopting project's own `AGENTS.md` still naming `CLAUDE.softeng.md`) is already detectable by the existing upgrade diagnosis against the current template, so it needed nothing. Global-context staleness is the real gap: only `personal-preferences.md` carried a version tag, 1 of 12 bootstrap files, so a developer's copied addendum files could never be found stale, and 0.15.0's rename of those exact filenames is a live instance of that biting. Gave the six copied files the same tag `personal-preferences.md` already uses rather than inventing a mechanism, and wired it into both procedures that make a tag mean anything: the bootstrap step now stamps each with a real snapshot on copy, and `upstream-check.md` now checks each file's tag separately rather than only the one an agent happens to remember, since they are snapshots taken at different times and drift independently. The five `global-context/*.md` scaffolds were left untagged deliberately: a developer fills those in and owns them, so a tag there would assert a sync relationship that never existed and read as permanently stale.
An adopting project's session reported a mistake of its own, unprompted and in structural terms, producing two conventions neither this repo nor that session would have reached alone.
- Path-keyed memory mis-resolution was documented only as a reading risk (getting the wrong project's context) and is equally a writing risk. A session designated for one service but rooted in another project's directory resolved to that project's memory and filed a scope-boundary fact about itself there, in a space owned by whichever agent actually works from that project; that agent later found claims about its own role it had not written. `session-discipline.md` now requires confirming the resolved memory belongs to the project actually being worked on, not the directory happened to be rooted in, before writing any fact about which session you are or what you own, and keeping it in conversation or with the developer where those differ. Filed as the memory-side form of the ownership rule added earlier the same day.
- The same incident continued into the sharper finding: that session flagged a later correction of that file as tampering and a hostile reversal, when it was the owning agent fixing the record about its own role on the developer's instruction. The existing convention covered dismissing an unexplained change and not the opposite polarity, which is worse because it accuses a colleague. The rule deliberately does not say to judge more carefully, since "this content is false" and "this is a legitimate edit by an authorized party I cannot see" are indistinguishable from inside one session: the missing information is authorization, which is not in the file. It changes the output instead, describe what changed, say you did not write it, ask who did, and reserve tampering/reversal/hostile for actual evidence of intent, since an accusation about authorized work costs more than a neutral question and reaches the developer either way.
The identity design was validated in production within the hour, by the case that motivated it. A registered Member was unreachable because filtering by project returned zero candidates: verified against the workspace files that a runtime handle's prefix is the basename of the workspace's first-listed folder, so it identifies a window and never a project, and two workspaces here both list `agentics` first. `window` is now derived from that folder rather than named, which makes prefix filtering checkable instead of guesswork.
- Self-blindness is routed around rather than solved: a session is excluded from its own `ListAgents`, so `id` is peer-supplied. Asking a peer directly does not work, since an inbound message carries a socket and a display name rather than a handle, so the peer cannot tell which listed session is asking. The set difference does work, each session being absent from its own listing and present in the other's. Tested live rather than written up untested: confirmed this session is `agentics-06`, with independent corroboration that a second agentics-prefixed session is a distinct handle, which ruled out coincidence rather than just producing a plausible number.
- A peer complied with the new `id` field and then argued against it, which improved it. Its point that one `updated` cannot vouch for a stable identity and an ephemeral handle at the same decay rate was right and is now answered in the spec: `id` is advisory, refreshed at session start, and its sharpest form, that validating an id would cost the same `ListAgents` call the id was meant to save, does not hold because identity propagation already has the recipient echo back the id they were addressed by, so a reused handle is caught in the same exchange for one message.
- The same peer caught a propagation gap here: the field landed in the convention and not in the bootstrap template the live global file is copied from, leaving that file rejecting a field an adopter had just been told to write, which another agent doing index hygiene would have been correct to delete. Second occurrence of that same template lagging in three days; logged as the pattern, since `CONTRIBUTING.md` covers a new convention *file* being reachable from every index and says nothing about a *field* added to a schema that exists in more than one copy.
- Inherited two uncommitted roadmap edits written into this repo by the session that had been mistakenly treated as agentics. Validated rather than accepted, per the unattributed-changes convention: the structural finding is sound and would not have been found here, that `recency-tiebreak-withdrawn` binds an agent's own lookup while the recency judgment was reaching senders already made from a layer above it, so the shipped rule does not reach where the mistake originates. Corrected one factual error the identity confusion had introduced, an attribution of the misdirection to agentics when the receiving session was in fact Lectern's, and marked which parts are reported rather than verified here.
