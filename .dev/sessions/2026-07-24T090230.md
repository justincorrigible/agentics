Logged a low-priority roadmap idea, then diagnosed why a session-start greeting had stopped triggering the checklist in a long-running session elsewhere.

- `.dev/roadmap.md`: logged "Portable agentics for non-repo-access Claude surfaces" (claude.ai Projects, custom instructions), low priority. Distinct from Phase 3's personal-context portability: this is about porting project-level conventions to a surface with no project or repo at all
- `template/conventions/session-discipline.md` § Session-start signals: added long-thread context loss, whatever the agent's own harness-specific mechanism produces it (for Claude Code, an automatic compaction event), as an explicit signal. Diagnosis: it most likely dropped the literal trigger-phrase list from context, since what survives is a narrative of what happened, not the exact instruction text read once as file content, so a later greeting in the same thread read as plain conversation with no rule behind it. Noticing it happened is now the signal itself, not something to wait to also match a phrase-based trigger
- `testing/regression-checklist.md`: added `long-thread-context-loss-signal` as a re-verifiable scenario for the same fix
- `template/conventions/code-style.md` § Non-mutational: added a loop-with-reassignment example and its recursive equivalent, prompted by an agent in a downstream project writing exactly that shape despite the rule already being in context; the existing example only covered conditional object construction
- Asked whether the compaction-signal fix was really the first time a convention got scoped around one example instead of the property it was illustrating; it wasn't: `Property ordering` needed the same widening twice already, and `surface-unprompted-too-narrow` was the same shape on a different bullet. Added a `CONTRIBUTING.md` design principle naming the pattern, written for a human contributor too, and a matching `regression-checklist.md` entry. Also added a brief unprompted gap-scan to `CONTRIBUTING.md`'s publish checklist, making today's proactive-surfacing pattern a standing part of every release rather than something that only happens when directly asked. Applying that same check to the loop-example fix above (added concurrently, not yet checked against it) found it was a fifth instance of the same pattern; added its own `regression-checklist.md` entry too
- Amended yesterday's still-unpushed `0.10.0` commit to fold in everything logged today, rather than publishing a separate version: nothing had been pushed yet, so there was no shared history to disrupt. `CHANGELOG.md`'s `0.10.0` heading date moved to today, the day it's actually being committed
- Published `0.10.0`: force-pushed the rewritten history to `origin` and `softeng-fork` (`--force-with-lease`); every downstream repo's `synced:` SHA now points at a commit that no longer exists on the remote, exactly the case the new troubleshooting section covers
- Developer reported a new incident: a new contributor's local agentics clone was badly out of date, and their agents kept trusting it instead of checking online. Diagnosis: the existing freshness check only diffs against whatever remote a clone happens to track (`git log @{u}..`), which can't distinguish "genuinely current" from "current relative to a stale or unintended fork this clone happens to point at". Separately found the developer's own `~/.claude/projects.md` still marked agentics "sole-maintained, no freshness check needed", false now that a second contributor is active, fixed directly (personal file, not this repo)
- `convention-levels.md`, `global-context/projects.md`: added a remote-URL verification step before trusting any local-vs-remote diff (confirm the clone's configured remote actually matches the documented canonical URL first); removed the "sole maintainer, skip the check" framing generally, since that belief is exactly what silently stops being true with nothing to catch it if the check never runs
- `convention-levels.md`: added separate guidance, agentics-contributor-only, for when it's the contributor's *own* local clone that's stale rather than the project being checked: flag it once, don't switch into fixing it inline (real, separate work that would derail an unrelated session), record a reminder in the contributor's own memory to offer fixing it later instead
- `testing/regression-checklist.md`: added entries for both fixes above; also found and merged a real duplicate from the concurrent session's non-mutational loop-example entry (two near-identical entries for the same incident)
- Second incident reported: asked when committing the agentics-adoption changes separately, that same contributor's agent added the `Co-Authored-By: Claude` trailer anyway, despite having read `code-style.md`'s "No AI-tool attribution" rule earlier that same session. Its own account: a harness-level default commit template appends that trailer automatically, and it applied the default mechanically instead of checking the actual message against the project's rule at the point of writing it, the same "read a rule, then generate content later without re-checking it" gap `verify-conformance-not-structure` already named, here as a competing external default rather than an internal structure/prose split. Added an explicit clause to the rule: it overrides the agent's own default commit template, and reading it earlier doesn't substitute for checking the message against it at the moment of construction. Added a matching `regression-checklist.md` entry
- Writing up the incident above put the contributor's actual name into this file, twice, and into a `CHANGELOG.md` entry, caught only on review before committing. The pre-commit consistency check couldn't have caught it: it only greps for the operator's own identity markers, known in advance, not a third party's name, which isn't known until it's written. Scrubbed all three instances; added an explicit note to `session-discipline.md` § Name code, not people and `testing/consistency-checks.md` that this stays a judgment call at the point of writing, plus a `regression-checklist.md` entry
- Asked for ideas on making agentics better generally; logged two lower-effort ones to `.dev/roadmap.md` (a new-contributor bootstrap prompt, whether `testing/fixtures.md`/`cold-read-review.md` are actually being exercised) and implemented the two mechanical ones now:
- `check-consistency.sh`'s "Root/template AGENTS.md section drift" upgraded "Interaction parameters" from a bullet count to a real content diff, exactly the shape that missed today's "baked in" vs "baked into code" drift. Running it for real against "Critical constraints" surfaced that section's content is *meant* to diverge (root is repo-specific, template is generic-adopter), so it kept the looser count-based heads-up instead of a strict diff that would've been permanent false-positive noise, caught before shipping an overly strict version
- `check-consistency.sh`: added a `python3` word-overlap heuristic flagging near-duplicate `regression-checklist.md` entries, heads-up only, the exact shape of the loop-example duplicate found earlier today. Verified against a synthetic reintroduced duplicate (77% overlap, correctly flagged) before trusting it
- A colleague's suggestion (priming review prompts with "assume there are mistakes," from Anthropic's own bun rewrite) mapped onto the developer's own "the ultimate question" habit and a gap in `AGENTS.md`'s existing "Sanity check requests" bullet (an output requirement, "surface gaps plainly," without ever stating the starting search stance that makes the search itself thorough). Generalized rather than scoped narrowly to `review-conduct.md`, matching yesterday's `property-scoping-recurs` lesson: one new `AGENTS.md` bullet states the posture generally (assume something's findable, paired with a "must concretely matter" calibration against manufactured findings), `review-conduct.md` and the developer's own "the ultimate question" memory entry now point back at it instead of holding independent copies. Added a matching `regression-checklist.md` entry
- `.dev/roadmap.md`: logged a new idea, proactive scope/effort estimation before large tasks with incremental checkpointing as the default execution shape, so a task risking interruption (Claude Code's usage limits are the motivating case) gets flagged and broken into individually-valid steps rather than attempted as one large uninterruptible push. Explicitly generalized past the Claude-specific framing to interruption risk in general, per the same lesson `property-scoping-recurs` already named
- `.dev/roadmap.md`: a second new idea, project-specific "task checklists" for recurring task types that touch multiple artifacts together (Arranger's search-server: a new env var should update both `.env.schema` and the README), turned out to be the same underlying gate as the existing "Session-completion checklist" item, just at a finer, per-task-type grain rather than universal fixed categories. Merged into one entry, "Definition of done: universal and project-specific," rather than left as two items that could drift apart
