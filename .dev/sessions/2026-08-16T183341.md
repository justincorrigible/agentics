Cross-project agentics audit (imicroseq/portal-ui, arranger, stage, both helm-charts repos), a repo-wide incident-citation review, and shipping the agent-index as a real convention.

- Audited `virusseq-portal`'s 0.10.0-to-0.14.0 sync: one real content drop found (a full sentence missing from the "Default to project-scoped" paragraph), one pre-existing gap carried forward correctly (missing "Working in a specific role" dispatch line, present since original 0.10.0 adoption). Compared against `arranger`, `stage`, and both `helm-charts` repos: the identical sentence was independently missing in all three projects synced against the same 0.14.0 commit, while both projects still at 0.13.0 had it intact. Root cause: `upgrading-adoption.md`'s "verbatim, don't paraphrase" rule only ever applied to the dispatch table; nothing said the same for any other section, so a CHANGELOG-mediated reconciliation naturally reconstructs additions from summarized prose rather than copying the current template's literal text.
- Ran a functional-value pass on all 19 "confirmed directly" incident citations across the repo (prompted by a direct challenge to one specific instance): 15 hold up (teach a specific gotcha, cite real data, or ground a genuinely counterintuitive claim), 4 didn't. Trimmed the 3 that were pure narrative restating an already-clear rule (`AGENTS.md`/`template/AGENTS.md`'s "Default to project-scoped" paragraph, `upgrading-adoption.md`'s "update"-ambiguity citation).
- Shipped the agent-index design (live-tested since 2026-08-08) as a real convention: `conventions/agent-index.md`, a bootstrap template at `global-context/agent-index.md`, a new dispatch line and initialization question (`agent_index: yes | no`) in both root and template `AGENTS.md`. Two confirming incidents drove this specifically: a session needing to reach Arranger hit two live same-project candidates with no way to disambiguate, resolved only through an out-of-band developer handshake; a second, unrelated session hit the identical ambiguity and asked the developer directly, when the index already had a correct, current Members entry that would have resolved it instantly, since nothing shipped anywhere told any agent to check it first. The shipped convention states that check explicitly as its own rule. Also refined during drafting, per direct instruction: Requests-entry cleanup ownership moved from the requester to the responding agent (already engaging with the mechanism at that moment), with the requester's own staleness check as a fallback, not the primary path.
- Ran the required fixture dry run (shape 4, steady-state mature adopter) against the dispatch-table and initialization changes before considering the agent-index work done: correctly surfaces both as a single non-conflicting batch, no bootstrap-artifact violation, registration trigger fires correctly once answered. Scratch fixture deleted after.
- Pruned two `.dev/roadmap.md` items now that they're shipped or substantially answered: "Family registry" (fully superseded by the shipped agent-index) removed; "Agent manager" and "the volatile-facts tier" updated to point at the shipped convention rather than "learned about, not yet used."
- `CHANGELOG.md` and `testing/regression-checklist.md` entries added for the agent-index shipment, the incident-citation pass, and the `atlas-roadmap-path-drift` fix (a path typo in `documentation.md` caught by real feedback from an imicroseq/portal-ui sync). All still sitting in `## Unreleased changes`, not yet published, per the standing "publish everything together at end of session" instruction.
