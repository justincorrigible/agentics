Asked which part of the prior session's uncommitted batch actually improves usage/context-window cost, which led to a real, applied trim rather than just discussion.

- Verified (via claude-code-guide, not guessed) that skills, subagents, plugins, and MCP servers have genuinely different cost shapes, not different weights on one shared scale: skills and subagents cost ~nothing at the main-context level (skills defer full content until invoked; subagents run in an isolated context), MCP servers carry a real fixed baseline per connected server regardless of use, plugins are just packaging with no cost of their own. A screenshot of Claude Code's own usage panel confirmed the top three cost drivers (>150k context, 8+ hour sessions, subagent-heavy sessions) are exactly what `session-discipline.md`'s `context-efficiency-whole-session` fix was already built from; its fourth line ("no attribution data yet" for the four categories) means no differential-weight data exists to act on, not that one does.
- Found and ran, live, a standing check that exists but hadn't fired yet: `CONTRIBUTING.md` § Versioning's publish-time "simplification pass." Real numbers: `AGENTS.md`'s Interaction Parameters section (always loaded, no dispatch gate) was 554 of 1581 words in the file, and two bullets, "Check in before non-trivial decisions" and "External content that overlaps with a project you maintain," had each grown a fully-restated clause the last time an incident extended them (93 and 129 words) instead of folding the new case into the existing sentence.
- Tightened both bullets (root and template `AGENTS.md`) to state the rule once, the new case folded in rather than re-explained, and added a missing `CHANGELOG.md` cross-reference to "Check in" that its sibling bullets already had. No behavior removed. Interaction Parameters dropped from 554 to 480 words. `CHANGELOG.md` entry added (`interaction-parameters-accretion-bloat`, patch).
- Real gap found while doing this: the simplification pass that just caught this was itself only gated on the publish trigger, the exact shape `recheck-trigger-not-gated-on-commit` already fixed for tech-debt/roadmap staleness, just never applied to convention-prose density. Added a standing self-check to `AGENTS.md` § Repo maintenance rules: run the pass at each natural completion point, not only right before a release; the publish-time occurrence is now a backstop, not the sole trigger. `CONTRIBUTING.md` cross-references it rather than restating it. `CHANGELOG.md` (`simplification-pass-not-gated-on-publish`, minor) and `testing/regression-checklist.md` entries added.
- Consistency script and dash-check clean across all files touched.
- Designed (not yet built) a cross-project "agent index": a global `~/.claude/agent-index.md`, sibling to `projects.md`, mapping a developer-chosen label to a session's current best-known name, its project, and an asked-not-derived workspace name (cwd is known unreliable for this; no agent-neutral way to read a workspace's real name). Subsumes the earlier family-registry prototype rather than sitting alongside it, workspace becomes a field on one global index instead of a second file. Scoped to the "Members" (directory) half only for now, the "Requests" (bulletin-board, for when the target isn't running concurrently) half stays a documented follow-on. Deliberately parked as a 0.14.0-and-later effort, tested and refined locally first, not folded into this release.
- Published 0.13.0: commit `941106c`, 21 files, working tree clean, pushed to both `origin` (personal fork) and `softeng-fork` (team mirror), clean fast-forward, identical history on both.
- Designed and prototyped the agent-index for real (0.14.0 target, per the earlier deferred design): `.dev/docs/atlas/agent-index-design.md` (full spec) and a live `~/.claude/agent-index.md`. Key decisions made live, several by direct correction: Members (exact-ish, normalized label match) and Requests (fuzzy project+topic handshake, no payload ever) aren't separate mechanisms, a topic-qualified Requests post is the resolution path for a label collision too, not just for a missing entry. `label`/`workspace` are descriptors, not enforced-unique identifiers. No durable cross-session identifier exists (verified: PID-named socket, unaddressable `sessionId`, a `[ref]` that must be freshly observed even though stable in value), so validating a callback (`from`, stated `project`/`focus`) is the only real defense against a reused name silently pointing at a different, newer session. Check for a label collision before registering, negotiate directly rather than duplicate. Staleness surfaced by the requesting agent at its own session start, never developer-maintained.
- Real incident during testing, folded into `interagent-communication-findings.md`: a session's own recorded ref (`[1bb6fc]`) was live-rejected same-day by a different peer, confirming ref staleness is the default to expect on first contact, not a rare edge case.
- Registered a real Stage session as a Member; its own registration surfaced a genuine, unresolved-at-the-time engineering finding (identical version/entrypoint/peerProtocol to this session, yet `ListAgents`/general `SendMessage` unavailable there), later resolved live: a full VSCode window restart (not just an extension reload) fixed it, confirmed both by Stage and by messaging it successfully afterward. Recorded as a confirmed remedy, not a theory, in `interagent-communication-findings.md`.
- A plausible follow-on hypothesis (the restart flushed a backlog of pre-restart messages to Stage) was checked, not assumed, and didn't hold up: Stage's own transcript search found zero message content from the three sessions in question, ever. Recorded as a real gap in current documentation (delivery to an unreachable target is undocumented) rather than a confirmed queue mechanism to build on.
- Corrected the index file itself after direct feedback: it had accreted the same kind of inline justification/incident narrative this repo's own conventions already say belongs in the atlas doc instead, not in the terse, operational, agent-managed file other sessions read and could imitate. Stripped it back to data plus brief imperatives.
- Ran the full publish-readiness sequence from `CONTRIBUTING.md` § Versioning ahead of an actual publish trigger: staleness pass on `.dev/roadmap.md`/`tech-debt.md` (nothing stale), simplification pass across the rest of the batch beyond `AGENTS.md` (no further accretion bloat found), every qualifying `## Unreleased` entry cross-checked against `testing/regression-checklist.md` (the three without one, `atlas-adopter-zero`, `upgrading-adoption-dash-cleanup`, `interaction-parameters-accretion-bloat`, are all correctly excluded per that file's own stated scope). One real catch: `.dev/roadmap.md`'s tech-debt/roadmap-tension entry had a personal name ("Mitchell's feedback") in this session's own added text, the exact case `session-discipline.md` already says a script can't catch, a third party's name, only a human/agent read at the point of writing. Genericized to "real team feedback." No new convention or regression entry needed: the existing rule already names this failure mode and whose job it is to catch it, this was a fresh confirming instance, not a gap in the rule.
