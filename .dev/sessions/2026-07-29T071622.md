Validated an unrequested change from a separate concurrent session on the same clone, then closed the gap that let it go unnoticed.

- The developer flagged that a separate, concurrent session had modified `template/conventions/testing.md` and `CHANGELOG.md` without going through this thread. Validated both: technically accurate (workspace `file:` dependencies resolve through built output, not source; `npm run` scripts lose `**` globstar recursion under non-interactive `sh`), no dashes, no personal info, no credentials, no duplication, `check-consistency.sh` clean. Kept, per the developer's go-ahead.
- Added a `git status` step to `session-discipline.md` § Starting a session, and a new "Unattributed working-tree changes" section: uncommitted content this session didn't make isn't evidence it's correct, validate it the same way any unverified claim is, and leave the keep-or-revert decision to the developer rather than the content's origin. Fixed one pre-existing, unrelated em dash on the line being renumbered. Added a `regression-checklist.md` entry
- While confirming the consistency script picked up the new untracked session file, found it never actually scanned untracked files at all: the personal-info check only reads `git diff --name-only HEAD` and staged files, missing any brand-new file, which for a `.dev/sessions/*.md` file means exactly the highest-risk window (before its first stage) went unchecked. Added `git ls-files --others --exclude-standard` to the scanned list; `testing/consistency-checks.md` updated to match
- An Arranger agent's commit carried a `Co-Authored-By: Claude` trailer; it reported never having opened `code-style.md` that session, since its work was scoped to docs and tests, and the dispatch table only routes there under "Writing code" or "Reviewing a PR or change." The developer pushed past the dispatch-scoping patch to the deeper issue: a general (non-dev) user is still a git user, so bundling universal rules into a dev-framed file was always going to be wrong, not just under-dispatched. Split `code-style.md`: Dashes, Spelling and language convention, Language and typos, and Property ordering moved to a new `conventions/writing-style.md`, read unconditionally by "Starting a session"; Git (commit safety, no AI-tool attribution) folded into `session-discipline.md`, already read the same way. `code-style.md` now holds only genuinely code-specific content. Root `AGENTS.md` had the identical gap in a more extreme form (never pointed at `code-style.md` at all), fixed the same way. Updated `template/README.md`'s file table and `.dev/roadmap.md`'s "Dev vs. non-dev users" item to note the split. Added a `regression-checklist.md` entry
- Ran a full refinement pass across all nineteen uncommitted files (conventions, `CHANGELOG.md`, session logs, tooling docs, global context) against `documentation.md`'s cold-reader and say-it-once rules: split run-on and comma-spliced sentences, fixed a stale `code-style.md` cross-reference, moved an aside that had broken a numbered list's continuity, fixed one comment in `check-consistency.sh` that no longer matched the code it described. `.dev/sessions/2026-07-28T114921.md` and `.dev/sessions/2026-07-24T090230.md`, both now prior-day closed files, were checked for Critical Constraint violations only; none found, left untouched
- `.dev/roadmap.md`'s "Definition of done" item: added `CHANGELOG.md` (if the project keeps one) as a fifth universal category, and a final refinement pass over both docs and changes as a sixth, the same gate `session-discipline.md`'s "Refinement passes" section already names rather than a new mechanism
- Re-read all three uncommitted session files and spot-checked their factual claims against actual repo state (git log, remote config, file contents) rather than trusting the prose: confirmed accurate, nothing stale or contradicted
- A genuine "what are we missing" pass across today's `code-style.md`/`writing-style.md` split turned up a real gap: `testing/README.md`'s own stated practice requires a fixture dry run before considering a dispatch-table change done, which never happened before today's edits were made. Built a disposable fixture (steady-state adopter shape) and ran a fresh, context-free subagent through a documentation-only task ending in a commit, exactly the original incident's shape. Confirmed the fix works end to end: it read `writing-style.md` unconditionally, never opened `code-style.md`, and the drafted commit message carried no AI-attribution trailer. Fixture deleted after, per its own disposal rule. Broader repo-wide grep for stale references to the five moved sections found none beyond `CHANGELOG.md`'s own historical entries (correctly describing a past file shape, not stale)
- The developer pointed out "is this done?" is literally "the ultimate question" asked at the completion checkpoint specifically. `AGENTS.md`'s "Default review or audit posture" bullet now names it as a recognized trigger phrase alongside "does this look okay"; `.dev/roadmap.md`'s "Definition of done" item notes the convergence is resolved, asking the question is what will invoke the checklist once it exists, not a separate mechanism
- An Arranger agent's proposal to add object/property key ordering to `code-style.md` turned out to already exist, in `writing-style.md`'s "Property ordering" section, moved there the same day; the proposal hadn't checked existing coverage first. Two of its refinements were genuinely new, though: a semantic-order exception (a reducer's accumulator, an enum-like ordered sequence, where alphabetizing would destroy something deliberate) and conditional spreads mixed with plain keys being a specifically higher-risk spot for the drift the section already warns about. Folded both into the existing section
- Arranger's `.dev/docs/release-process.md` (branching, versioning, a semver-judgement framework, Jenkins mechanics) is the source document `.dev/roadmap.md`'s release-process item was waiting for. It explicitly isn't ready to template yet (its own § 9 lists five open questions, and it's validated against one round so far); noted its generalizable patterns and open questions in that roadmap item rather than drafting a convention now, given the scope warrants its own dedicated design pass
- Discussed that document further: confirmed it's a project-facing convention (Arranger as proving ground), not agentics' own release process, and that the Jenkins pipeline is ancillary context, branching/merging is the actual documentation target. Investigated whether Changesets (raised in Arranger's own roadmap to replace the manual version-bump process) is compatible with keeping `main` fully free of versioning state: verified against Changesets' current docs that a changeset file's frontmatter holds package name → bump type only, never a version number, but the documented workflow assumes changesets accumulate and get consumed on the same branch, with a real, unproven risk if split across branches (a consumed changeset's file deletion not reconciled back onto the code branch, causing conflict or reintroduction on the next promotion merge). Logged in the roadmap item as needing a real prototype before trusting it. Drafted a self-contained prompt for a separate Arranger session to run that prototype
- The developer ran that prototype directly in Arranger (`.dev/docs/release-process.md` § 9, real disposable two-package monorepo, real `git merge`/`changeset version` runs) and updated the source document with the findings: the branch-split works, the predicted reintroduction/conflict risk does not occur in the ordinary case (git's 3-way merge auto-resolves the delete-vs-unmodified difference), and the one real edge case (editing an already-consumed changeset on `main`) produces a trivially-fixable `CONFLICT (modify/delete)`, cheaply preventable with a CI lint rule. Updated `.dev/roadmap.md`'s "Release process convention" item to record this as resolved rather than an open risk, and fixed a stale `§ 9` cross-reference to `§ 10` after the source document's own section numbering shifted to make room for the new prototype section
