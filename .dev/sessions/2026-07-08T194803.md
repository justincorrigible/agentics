Replaced the single shared `.dev/sessions.md` with per-contributor-per-day files under `.dev/sessions/`, after a downstream project (Arranger) hit repeated merge conflicts from concurrent contributors editing the same file; filename uniqueness (timestamp, no descriptive slug) is what prevents the conflict, not smaller files or grouping by name. Also closed a related gap: a global agent context built on agentics was found to be duplicating conventions the template already defines, which drifts silently when only one copy is updated.

- `template/conventions/session-discipline.md`, `template/AGENTS.md`: added "Session file identity" section: one file per (day, contributor), named `YYYY-MM-DDTHHMMSS.md`; authorship for "is this file mine" is resolved via `git log`/`git config user.email`, never written into file content; removed the newest-to-oldest single-file ordering rule
- `template/README.md`, `template/DEVELOPMENT.md`, `template/CLAUDE.roles/ai-eng.md`, `template/CLAUDE.roles/bio.md`, `template/CLAUDE.roles/general.md`, `template/conventions/convention-levels.md`, `template/conventions/security.md`, `template/conventions/security-guidelines.md`, `template/global-context/roadmap.md`, `template/global-context/security-guidelines.md`, root `README.md`, `AGENTS.md`, `CLAUDE.md`: updated references from `.dev/sessions.md` to `.dev/sessions/`
- `template/conventions/convention-levels.md`: added guidance that a global context built on agentics should reference conventions it already covers rather than duplicating them, keeping only genuinely local refinements or stated overrides
- `CHANGELOG.md`: logged `sessions-per-contributor-file` (breaking: downstream adopters must migrate their own `.dev/sessions.md`) and `global-dispatch-not-duplicate` under Unreleased
- `.dev/sessions.md`: retroactively split into `.dev/sessions/*.md`, one file per historical date (single-contributor history, so no per-day disambiguation was needed); old file removed

Added a `Repo URL` field to the personal cross-project map so agents prefer reading a local clone over fetching its GitHub URL, since local is often ahead of origin (uncommitted or unpushed work); paired with a freshness check (`git log @{u}..`) for repos with other contributors, skipped for sole-maintained ones. While populating it, found `softeng/agentics`'s local `origin` remote points to a personal fork rather than the `oicr-softeng/agentics` org repo this repo's own docs reference as canonical: flagged for the developer to confirm, not resolved here.

- `template/global-context/projects.md`: added the same `Repo URL` field and rule to the shareable template, so other agentics adopters get it too
- `CHANGELOG.md`: logged `repo-url-local-first` under Unreleased

Implemented the Phase 2 "update propagation" roadmap item that had been open since the repo's founding: adopting projects now get an automatic, session-start check for agentics updates instead of a manual CHANGELOG comparison, reusing the `Repo URL` local-first lookup and the propagation-suggestions opt-in already in place rather than inventing new machinery.

- `template/conventions/convention-levels.md`: new "Checking for upstream updates" section: adopted files carry `<!-- agentics-template-version: X.Y.Z | synced: <commit-sha> -->`; session start diffs `CHANGELOG.md` since that commit in the local agentics clone (skips entirely if nothing changed), surfaces `breaking: yes` entries first, applies per-entry with no bulk auto-apply, advances the marker after review; missing `synced` values baseline to current `HEAD` instead of a full historical diff
- `template/conventions/session-discipline.md`, `template/AGENTS.md`: added a session-start checklist step wiring in the above check
- `README.md`, `template/README.md`: rewrote "Keeping up to date" to describe the new tag format and automatic check
- `.dev/roadmap.md`: Phase 2 update-propagation item resolved, removed
- `CHANGELOG.md`: logged `upstream-update-check` under Unreleased (breaking: the tag format itself changed shape)

Audited the template against its own "Agent-neutral" design principle (CONTRIBUTING.md), prompted by noticing how much of today's own work leaned on Claude-specific mechanisms; found three violations that had drifted in despite the principle already being written down, one of them self-inflicted (the upstream-check step referenced `CLAUDE.md` specifically instead of `AGENTS.md`, the file actually guaranteed present for non-Claude agents).

- `template/conventions/convention-levels.md`, `template/global-context/projects.md`: replaced unconditional `~/.claude/...` references with the `(for Claude: X; for other agents: Y)` pattern already used correctly in `global-context/README.md`
- `template/conventions/security.md`: same fix, plus a stale `personal-context/` path left over from the 2026-06-05 rename to `global-context/`
- `template/conventions/session-discipline.md`, `template/AGENTS.md`: corrected today's own upstream-check step to check `AGENTS.md` (or `CLAUDE.md`), not `CLAUDE.md` alone
- `CONTRIBUTING.md`: added a `grep -rn '~/\.claude\|Claude Code' template/` step to "Proposing changes": the principle being documented didn't stop it drifting back, so the check is now mechanical rather than relying on a read-through
- `CHANGELOG.md`: logged `agent-neutral-drift-fix` under Unreleased

Closed two gaps in the upstream-update check found by walking through what would actually happen opening a fresh session in an unmigrated project (Arranger) right now, before found: (1) the "no synced value" branch baselined to `HEAD` silently, which meant the very first run on any project would have swallowed the current pending batch instead of showing it, the opposite of the point; (2) the check was gated on finding a well-formed tag, so a project with no tag at all (the actual state of every existing adopter today) would never trigger it, rather than being flagged as needing one.

- `template/conventions/convention-levels.md`: adoption is now detected via the tag in any form, or failing that a plain mention of agentics in `AGENTS.md`/`CLAUDE.md`; a missing/incomplete tag surfaces the current `## Unreleased` section as a bounded catch-up and offers to add the tag, instead of either being skipped or silently baselined away
- `template/conventions/session-discipline.md`, `template/AGENTS.md`: loosened the checklist gate to match
- `CHANGELOG.md`: revised the same-day `upstream-update-check` entry in place to describe the corrected behaviour (not a new entry: still today, still open for editing)
