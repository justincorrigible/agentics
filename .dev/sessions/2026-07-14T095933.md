A live adopter's agent (Arranger) logged a session file naming an individual, listing dead external commit SHAs, and narrating a PR review instead of its one real local change (a tech-debt entry). Two independent convention gaps confirmed and fixed.

- `template/AGENTS.md`: backfilled the entire missing § Session file entry format (bullet format, write-about-effects, name-code-not-people, immutability) — it jumped straight from timestamp rules to tech-debt format with none of this, so any agent relying on `AGENTS.md` alone never saw these rules
- `template/conventions/session-discipline.md`: added "Mixed reviews: log the local effect, not the investigation" to § Session file entry format, since "do not log conversational activity" only ever named the zero-local-change case, leaving the mixed case unaddressed
- `CHANGELOG.md`: logged `session-entry-format-agents-parity` under Unreleased, `breaking: yes`

A second finding surfaced once the offending file was rewritten in place, after its day had already closed, to remove the individual's name and dead references: that broke the letter of "a session file is immutable once its day is done," and was still the right call, not a violation of it.

- `template/conventions/session-discipline.md`, `template/AGENTS.md`: added "Exception: a Critical Constraint violation overrides immutability", naming the credential-in-an-old-commit analogy directly: immutability protects judgment calls from being quietly rewritten later, not a Critical Constraint violation (an individual's name, a credential) from being corrected regardless of which day produced it
- `CHANGELOG.md`: logged `immutability-critical-constraint-exception` under Unreleased
