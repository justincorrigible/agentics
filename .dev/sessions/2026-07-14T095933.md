A live adopter's agent (Arranger) logged a session file naming an individual, listing dead external commit SHAs, and narrating a PR review instead of its one real local change (a tech-debt entry). Two independent convention gaps confirmed and fixed.

- `template/AGENTS.md`: backfilled the entire missing § Session file entry format (bullet format, write-about-effects, name-code-not-people, immutability) — it jumped straight from timestamp rules to tech-debt format with none of this, so any agent relying on `AGENTS.md` alone never saw these rules
- `template/conventions/session-discipline.md`: added "Mixed reviews: log the local effect, not the investigation" to § Session file entry format, since "do not log conversational activity" only ever named the zero-local-change case, leaving the mixed case unaddressed
- `CHANGELOG.md`: logged `session-entry-format-agents-parity` under Unreleased, `breaking: yes`

A second finding surfaced once the offending file was rewritten in place, after its day had already closed, to remove the individual's name and dead references: that broke the letter of "a session file is immutable once its day is done," and was still the right call, not a violation of it.

- `template/conventions/session-discipline.md`, `template/AGENTS.md`: added "Exception: a Critical Constraint violation overrides immutability", naming the credential-in-an-old-commit analogy directly: immutability protects judgment calls from being quietly rewritten later, not a Critical Constraint violation (an individual's name, a credential) from being corrected regardless of which day produced it
- `CHANGELOG.md`: logged `immutability-critical-constraint-exception` under Unreleased

A different adopting project (helm-charts) surfaced a design-review gap: asked why a better schema wasn't offered unprompted (a redundant `type` discriminator instead of deriving it from already-present config), not just the narrow schema fix.

- `template/conventions/code-style.md`: added "Derive from data, don't duplicate as a flag" as a fourth bullet under § Conditional logic and functional style, generalized past Helm to API schemas and configuration objects broadly
- `template/CLAUDE.md`, `template/AGENTS.md`: reworded "Surface better alternatives" in § Interaction parameters to name the trigger explicitly (before presenting an implementation, not only after being asked why one wasn't offered) — it existed already but named no timing, so it only fired reactively
- `template/AGENTS.md`: found and fixed a second, unrelated propagation gap while there: the entire "Verify purpose alignment before implementing" bullet was missing from § Interaction parameters, present in `CLAUDE.md` but never backfilled
- `CHANGELOG.md`: logged `design-review-and-derived-flags` under Unreleased, `breaking: yes`

Onboarding a new contributor elsewhere surfaced a gap in the template itself: `template/DEVELOPMENT.md`'s Prerequisites and Setup were blank TODOs with no default filled in, not even for GitHub CLI auth, which any agent working in an adopting repo depends on for PR/issue work.

- `template/DEVELOPMENT.md`: added GitHub CLI (`gh`), authenticated, as a default Prerequisites bullet and a one-line Setup step; kept as documentation rather than a new onboarding-wizard question, since it's a one-time local check, not a preference the agent records in memory
- `CHANGELOG.md`: logged `development-md-gh-cli-prereq` under Unreleased

A live helm-charts adopter added a new `network:` key at its YAML edit point rather than its alphabetical position: the insertion-position instruction in the property ordering rule used "resource block" (Terraform terminology), leaving the equivalent case for plain YAML keys unstated.

- `template/conventions/code-style.md`: generalised "When inserting a new resource block" to "When inserting a new key or resource block", naming both edit-point and end-of-file as the wrong positions
- `CHANGELOG.md`: logged `property-ordering-insertion-scope` under Unreleased
