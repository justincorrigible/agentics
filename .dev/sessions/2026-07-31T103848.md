Three real findings surfaced this morning, one from the developer, two relayed from a different agent's session, all fixed with a concrete, verified basis rather than theory.

- `template/conventions/session-discipline.md` § Session-start signals: a plain greeting had stopped reliably re-triggering the checklist for some agents. Diagnosed as the same silent-exception pattern `contributor-check-explicit-override` already named (a rule that reads as mandatory losing to a model's own "this seems redundant" reasoning), worsened by this file and `AGENTS.md` both having grown denser recently. Added an explicit clause: fires every time, no judgment call about recency
- `template/conventions/writing-style.md` § Dashes: the no-dashes rule had no discrete action tied to it, only a description. A personal-global-context patch for this (after the `virusseq-portal-ui` #522 incident) recurred anyway on a multi-step task, proving the gap belonged in the shared convention. Added a mandatory `grep -c '—\|–'` check immediately before any output leaves the agent's control
- `template/conventions/review-conduct.md` § Ground truth over claims: a `gh api -f body=@file` CLI flag mistake (`-f` doesn't expand `@filename`, `-F` does) silently posted a literal filename string as 8 PR comments' bodies; none were read back before being reported posted. Added: fetch and confirm an external write's actual result before reporting it complete, and verify the first of a repeated batch before firing the rest
- `CHANGELOG.md` and `testing/regression-checklist.md`: entries added for all three
- `template/conventions/review-conduct.md` § Draft, never post: a fourth relayed finding, a draft PR reply approved and posted correctly as an inline thread reply, but its closing line ("Approving now!") implied a formal review action the text itself can't produce; the PR's review decision stayed unchanged, caught only by checking the platform directly. Added: state the draft's target (which thread, or a formal review action) alongside its wording before presenting it for approval, not just the wording itself. Folded into the existing section rather than a new one, matching the other agent's own recommendation. `CHANGELOG.md` and `regression-checklist.md` entries added; `check-consistency.sh` passes
