An Arranger agent's session log narrated the file's own current git staging state into itself, a new failure mode diagnosed and fixed in `session-discipline.md`.

- `template/conventions/session-discipline.md` § Git: added a sentence scoping "state git state plainly" to conversational replies, not persisted file content, since a claim like that goes stale the instant anything gets staged and the file never gets corrected after the fact
- `template/conventions/session-discipline.md` § Session file entry format: added a concrete example alongside "Write about effects, not style," matching the file's existing pattern for citing a real slipped-through instance verbatim
- `testing/regression-checklist.md`: added `git-status-narrated-into-persisted-file`
- `CHANGELOG.md`: logged the fix under `## Unreleased changes`

The same Arranger agent, asked to "update your own agentics," instead reached directly into this clone mid-session and wrote to it live, an ambiguous phrase resolved toward the more invasive reading instead of the safer local one.

- Confirmed via file modification times, not just git status: `template/conventions/review-conduct.md` was written at a timestamp this session never touched it. Two substantive additions were present, only one with a matching `CHANGELOG.md` entry
- Validated each: kept a live-system "ground truth" diagnosis paragraph (sound, already logged as `ground-truth-live-system-diagnosis`); removed an undocumented paragraph that duplicated `AGENTS.md`'s existing "Default review or audit posture" bullet almost verbatim, no CHANGELOG entry, no new content beyond what's already stated there; left a third, pre-existing paragraph alone (already accounted for by an earlier, dated entry)
- `template/conventions/convention-levels.md` § Propagation suggestions: reworded the "Agentics contributors" bullet so surfacing the agentics repo as a candidate stays always-on, but writing into it directly is a separate action requiring the session to already be working inside that repo, or an explicit instruction naming that exact action. Default is now stated explicitly: fix the immediate issue locally first, then offer the agentics-side version back rather than reaching into that clone
- `template/conventions/session-discipline.md` § Unattributed working-tree changes: added a CHANGELOG-completeness check to what "validate" means concretely, since a missing entry is itself a sign the addition bypassed this repo's normal review process
- `testing/regression-checklist.md`: added `propagation-crosses-repo-boundary-uninvited`
- `CHANGELOG.md`: logged the fix under `## Unreleased changes`
- `AGENTS.md` § Critical constraints: added a matching safeguard on this repo's own side, in case an agent ends up here anyway: no modifying any file without an explicit instruction naming this repository, and check whether an ambiguous instruction more likely means applying the conventions to the agent's own current project instead
- A requested pass over all uncommitted changes turned up two real, pre-existing dash-rule violations, both fixed: an em dash in `convention-levels.md`'s "Agentics contributors" bullet, a space-hyphen-space in `session-discipline.md`'s "Starting a session" step 3
- Tightened `AGENTS.md`'s new Critical Constraints bullet: the disambiguation step only applies when arriving from a different project, not when agentics genuinely is the session's own project
- Pre-publish check: `.dev/tech-debt.md` and `.dev/roadmap.md` had nothing stale to close. `testing/regression-checklist.md` was missing an entry for the already-logged `ground-truth-live-system-diagnosis` CHANGELOG fix (a genuine behavioral/interpretive fix, same as every other entry today); added it
- Traced where a "live pointer" conventions file actually resolves to for an adopting project: a bare relative path in the project's own dispatch table, resolved via the reading agent's own global context ("Repo URL" entry), confirmed by reading Arranger's `AGENTS.md` and `~/.claude/projects.md` directly. Found two related, previously-unaddressed gaps this surfaced: nothing said a live-pointer trigger firing again later in the same session means re-reading rather than reasoning from an earlier read; and the remote-verification step protecting the freshness check never gated the live-pointer read path itself. Added both to `template/conventions/convention-levels.md`, with regression-checklist and CHANGELOG entries. Logged a third, bigger question in `.dev/roadmap.md` without deciding it: whether an *other* project's live-pointer read should resolve to last-committed content instead of the raw working tree, since uncommitted drafts currently leak out exactly the way `rewrite-freely-until-committed` says they shouldn't
- While in `convention-levels.md`, fixed two more pre-existing, previously-shipped em dashes found in text being directly edited (`§ How much to keep locally`)
- Recorded the developer's own GitHub handle in `~/.claude/CLAUDE.md` and `~/.claude/projects.md` (personal global context, not this repo), paired with an actual instruction rather than a bare fact: recognize content authored under that identity as carrying in-conversation authority. Generalized the underlying mechanism (not the personal value) into `review-conduct.md` § "Ground truth over claims": an exception for content from an identity the developer's own global context records, with `regression-checklist.md` and `CHANGELOG.md` entries
- Audited agentics' own project memory (17 files) for the first time in a while: found four memories explicitly superseded by conventions this repo already ships, two misfiled under the wrong project entirely, two stale (one factually wrong about current file structure), and one (the remote-setup memory) that I should have applied a few turns earlier but didn't, retracted the resulting bad suggestion. Resolved the underlying "when does this get pruned" question into two separate fixes rather than one, since a contributor creates memory staleness in the same breath they ship the superseding convention, far more often than an adopter hits it via a template sync: `CONTRIBUTING.md` § Proposing changes gained a step to prune immediately after shipping; `convention-levels.md`'s upstream-update classification step gained the adopter-side equivalent. Logged both with `CHANGELOG.md` and `regression-checklist.md` entries, and closed out the roadmap item that had been sitting undesigned
- While in `convention-levels.md` and `CONTRIBUTING.md` for the above, found and fixed a further 13 pre-existing em dashes neither had anything to do with today's work, caught only because I was already editing those exact files. Found the same problem is repo-wide (14 files, `CHANGELOG.md` alone has 16) and that `check-consistency.sh` has no automated dash check at all, unlike personal-info; flagged as a separate, larger, not-yet-actioned finding rather than expanding scope unprompted
- Actually cleaned up the personal memory space per the audit: deleted the ten superseded, misfiled, or stale files; trimmed `user_andy_profile.md` to only its non-redundant content; promoted the two genuinely misfiled memories (macOS `sed` `\b` caveat, K8s sibling-namespace debugging pattern) into `~/.claude/CLAUDE.md`'s existing "Bulk text replacements" and "Kubernetes infrastructure conventions" sections respectively, rather than just deleting the insight along with the file; rewrote `MEMORY.md` to match; fixed one dangling `[[...]]` link the deletions created
- Logged a new `.dev/roadmap.md` item, "cross-pollination of expertise," from a real instance the developer ran directly: the Arranger agent wrote a `.dev/docs/arranger/README.md` inside Stage (verified against Stage's real code, not generic docs) plus a narrow dispatch line to reach it, surfacing one real actionable finding along the way and correctly leaving alone everything a separate concurrent thread in Stage had in flight. Logged as an idea with open questions, not designed yet
- Corrected the Overture docs-link rule in `template/CLAUDE.overture.md`: verified the developer's explanation and a Maestro agent's summary directly against `overture/docs`'s real config and several product repos' actual `docs/` content rather than accepting either at face value (should have checked existing agentics coverage first, per `review-conduct.md`'s own "check existing coverage" principle, applied to myself). The existing rule only documented the GitHub-URL form, silently wrong for the common case of linking to another product's actual published page. Restated as three cases, added real link breakage found while verifying as concrete review-time checks, with `CHANGELOG.md` and `regression-checklist.md` entries
- Logged a second, related but distinct roadmap item: intra-project knowledge consistency across different agent sessions working on different features of the same repo (not cross-project like the item above), informed by a real demonstration: asked an Arranger agent whether everything it knows is actually documented, and it correctly separated durable facts from reasoning that's meant to stay ephemeral, catching one real at-risk item (a PR draft sitting in a session scratchpad) along the way
- Worked out and folded into that same item: a concrete "worth documenting" test (would a different session working on a different part of the same project need this to avoid re-deriving it or contradicting a settled decision), and that memory vs. `.dev/docs` is pointer vs. substance, not a choice between two shelves
- Resolved the remaining open questions on that item through discussion: automatic, per-decision trigger (not a session-end sweep); a two-tier split (lessons learned write live, everything smaller stays in memory until asked or accumulated); folds into "Definition of done" as a sharpened criterion enforced by the existing "Refinement passes" checkpoints; universal, not product-family-specific. Updated the roadmap entry to match, since it had gone stale relative to the discussion. Still open: the actual convention prose itself, not yet written
- Implemented "Definition of done": new `conventions/definition-of-done.md` (universal checklist, project-specific annexes, the lessons-learned criterion in full), wired into both `AGENTS.md` dispatch tables and the "Default review or audit posture" bullet, `session-discipline.md`'s "Refinement passes" extended as the enforcement backstop. Fixture-dry-run tested (required, since it touches the dispatch table): a fresh subagent correctly followed the new dispatch, applied the checklist against real project state rather than assuming it clean, and gave a genuine "no, not fully" answer rather than a rubber stamp. A second fixture round surfaced a real, honest limitation: the live per-decision lessons-learned capture didn't fire proactively without being asked, worth revisiting later, not silently claimed as working
- Compared the developer's personal global `~/.claude/CLAUDE.md` against agentics section by section (not from memory) after the developer asked how much of it could become pointers to reduce duplication. Found several sections were exact or near-exact duplicates, and several more had gone stale, missing real improvements agentics had already shipped (Property ordering's semantic exception, Interaction parameters' three newer bullets, Git's two newer rules). Converted the confirmed duplicates to the same dispatch-plus-local-refinements pattern the file's own "Session discipline" section already used, preserving every genuinely personal addition as a local bullet. File went from 331 to 195 lines
- Found and fixed one real agentics-side gap surfaced by that comparison: `code-style.md`'s "Checking in before non-trivial decisions" was only reachable via the "Writing code" dispatch trigger, despite applying to any non-trivial decision. Moved it into `AGENTS.md`'s "Interaction parameters" (root and template kept byte-identical), read unconditionally every session. `CHANGELOG.md` and `regression-checklist.md` entries added; the developer's personal `CLAUDE.md` updated to match (folded its own now-redundant "Checking in" section into its existing Interaction parameters dispatch)
