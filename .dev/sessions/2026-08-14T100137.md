Resumed after a usage-limit gap. Found and fixed a real bookkeeping inconsistency, then reviewed a contributor's own global setup for propagation candidates, per a direct request to make that a standing practice.

- Reconciliation: the prior 0.14.0 release commit (rewritten by Git Tower's auto-stash/rebase to a different hash than originally recorded, content-verified unchanged) had actually shipped three follow-up entries (`personal-preferences-no-project-scaffolding`, `global-tag-version-tracking`, `quick-start-sequential-not-additive`) to both remotes, but they were left sitting in `## Unreleased changes` instead of moved under the `### 0.14.0` heading. Folded them in properly; `## Unreleased changes` is clean again.
- Reviewed one contributor's own global context for candidates never surfaced upstream, six confirmed genuine after checking each against current agentics content (not assumed missing): `code-style.md` gained the OWASP A09 structured-logging justification, "verify before assuming a limitation" extended to the agent's own tooling, and a generalized "check for a working sibling instance before diagnosing in isolation" heuristic. `session-discipline.md` gained a rule against trusting a prior summary's claims of completed work without verifying, and a default-to-feature-branch git-push rule. `review-conduct.md` gained a functional-value test for when a user questions whether a convention is worth following, generalized past any one user's own stated tendency.
- The more general fix: `convention-levels.md` § Propagation suggestions now runs a reverse check in the same pass as the existing mandatory upstream-check, for `agentics_contributor: yes`, review your own global context's local refinements against current agentics content and surface genuine candidates, not just react to fresh discoveries. This was the actual gap the six candidates above existed as a symptom of: nothing previously reviewed a contributor's already-established setup at all.
- `CHANGELOG.md` entries added for all four (`code-style-propagated-verification-heuristics`, `session-discipline-propagated-safety-defaults`, `convention-skepticism-functional-test`, `proactive-reverse-propagation-check`); regression-checklist entries added for all four, each genuinely behavioral. Dash-check and full consistency script clean throughout. Sitting in `## Unreleased changes`, not yet published.
- Extended the agent-index design (`.dev/docs/atlas/agent-index-design.md`, still a local prototype, not shipped) with a draft for noticing a pending Requests entry without being reminded: an urgency signal folded into the existing `re:` field as an optional opener (`re: urgent. topic`) rather than a new schema key, a two-tier check trigger (an idle/background mechanism if the runtime offers one, task-boundary checkpoints always), and an acknowledge-don't-switch-always-tell-the-developer rule for finding something mid-task. Framed capability-gated and agent-neutral throughout, matching the rest of this design's own gating discipline.
- Caught live, presenting that design-doc edit as complete without ever running `definition-of-done.md`'s checklist: `AGENTS.md`'s dispatch table already names two triggers ("finishing a task, or asked 'is this done?'"), but the checklist's own "Invoked by" line only documented the second. Fixed `definition-of-done.md` to state both explicitly, with this incident as the confirming case; `done-pass-self-trigger` added to `CHANGELOG.md` and `regression-checklist.md`.
