Propagated a Dashes convention refinement from the developer's personal global config into the template, closing an ambiguity the template's own text admitted was unresolved.

- `template/conventions/code-style.md`: § Dashes rewritten to explicitly name structural exceptions (bullet markers, horizontal dividers, table separator rows, compound words, numeric ranges) instead of leaving space-hyphen-space vs. bullet-prefix as case-by-case review; added a heading/title example (colon, not dash) that wasn't covered before
- `~/.claude/CLAUDE.md` (outside this repo): same refinement, plus the table-separator addition originated here first; also fixed a stale `sessions.md` reference in the bullet-annotation example to `.dev/sessions/`, found while porting the text over
- `CHANGELOG.md`: logged `dashes-structural-exceptions` under Unreleased

Real confusion while working with an adopted project (Arranger, a different session): the developer relies on git staging to review changes incrementally and discard what doesn't work, expected staging to happen automatically, and hit a moment of "you said there's changes but git shows nothing" when it didn't. Root cause: agents don't stage without being explicitly asked, same as they don't commit, but that constraint was never actually stated anywhere, only "never commit" was.

- `~/.claude/CLAUDE.md` (outside this repo): added explicit staging restraint next to the existing "never commit" rule, plus a requirement to state a change's actual git state (staged vs. unstaged) plainly rather than just confirming it exists
- `template/conventions/code-style.md`: filled in § Git, previously an empty team placeholder, with the same real default guidance rather than leaving it as an example to configure
- `CHANGELOG.md`: logged `git-staging-transparency` under Unreleased

Brought a fully worked writing-register convention, root-caused via real feedback from a dev and a non-dev team member on Arranger's docs: dense, backward-referencing prose written during/after a live design discussion works for whoever was in the room and against anyone reading cold, the actual audience for most documentation. Correctly identified as a register problem, not a density problem, with a before/after example and explicit scope (design docs/tech-debt/roadmap, not `.dev/sessions/` logs, which stay terse on purpose).

- `template/conventions/documentation.md`: added "Writing for a cold reader" with five rules (one idea per sentence, conclusion first, restate don't reference, examples in their own block, minimize cross-reference chains); retitled the file from "Documentation structure" to "Documentation" since it now covers more than structure
- `.dev/tech-debt.md`: logged that `AGENTS.md` has no dispatch to `documentation.md` at all, found while placing this addition; standalone, not fixed in scope
- `CHANGELOG.md`: logged `writing-for-a-cold-reader` under Unreleased

Closed the `AGENTS.md`/`documentation.md` gap just logged, then widened the check per the developer's request: is there anything else CLAUDE.md dispatches to that AGENTS.md doesn't cover, and anything in conventions/ that CLAUDE.md doesn't dispatch to at all.

- `template/AGENTS.md`: added "Documentation" (two-tier model plus a condensed writing-for-a-cold-reader summary) and "Deploying or debugging a service" sections, both previously entirely absent despite being in `CLAUDE.md`'s dispatch table
- `template/CLAUDE.md`: "Security-relevant work" now dispatches to `conventions/security.md` as well as `security-guidelines.md` — `security.md` (credentials policy, Node.js/pnpm supply-chain hardening, quick threat model) was never referenced from the dispatch table at all, making its pnpm guidance genuinely unreachable since it was added
- `template/AGENTS.md`: Security section updated to match, with a condensed pnpm supply-chain summary inlined
- `template/README.md`: file table was missing `code-review.md`, `documentation.md`, and `upgrading-adoption.md` entirely; added
- `.dev/tech-debt.md`: removed the now-resolved AGENTS.md/documentation.md entry
- `CHANGELOG.md`: logged `fix-orphaned-references` under Unreleased (breaking: adopters who copied the dispatch table before this fix are missing the security.md line and the two new AGENTS.md sections)

Asked whether human-facing docs needed the same treatment. `template/README.md` was already covered by the fix above; the real remaining gap was procedural, not a specific doc: nothing stops the next new `conventions/*.md` file from falling into the same three-way hole.

- `CONTRIBUTING.md`: added a step to § Proposing changes: a new `conventions/*.md` file must be wired into `CLAUDE.md`'s dispatch table, `AGENTS.md`'s inline coverage, and `template/README.md`'s file table, naming the three real instances this already happened to
- `CHANGELOG.md`: logged `new-convention-file-checklist` under Unreleased

A real incident during a different new project's initialization (overture/docs): an agent generated a tech-debt entry that skipped yesterday's "separate issue from fix" rule, matching the format's field skeleton exactly while missing the prose requirement next to it. Asked directly why, its self-diagnosis found two distinct causes, both real: the skeleton itself never got a `fix:` field added when the rule was written, and more generally, reading a rule and applying it several steps later in the same turn are not the same act, with nothing forcing the second to happen in a multi-artifact batch like initialization.

- `template/conventions/session-discipline.md`, `template/AGENTS.md`: added an explicit `fix:` line to the tech-debt skeleton
- `template/conventions/session-discipline.md`, `template/AGENTS.md`: added "Verifying conformance, not just structure": re-read each governed artifact against a convention's specific prose requirement before finalizing, as a discrete step, especially across a multi-artifact batch, not assumed satisfied from having read the rule earlier
- `CHANGELOG.md`: logged `tech-debt-fix-field` and `verify-conformance-not-structure` under Unreleased

Corrected a wording risk in the `fix:` field I'd just added: as first written, it read as banning a pointer to where the fix lives entirely, when the actual requirement is narrower — a pointer with no inline substance at all is what's disallowed, not redirecting for full detail once the fix itself is stated in one sentence.

- `template/conventions/session-discipline.md`, `template/AGENTS.md`: reworded the `fix:` field spec and its prose to say explicitly that redirecting to a roadmap item or design doc for depth is valid and often correct; what's required is a one-sentence statement of the fix itself alongside it, not a bare reference standing in for the field
- `CHANGELOG.md`: revised the same-day `tech-debt-fix-field` entry in place to match (not a new entry: still today, still open for editing)

Documented a design question the developer raised: for a brand-new adopter with no local agentics clone, or a contributor whose other projects shouldn't duplicate agentics unnecessarily, what actually needs to be kept locally? Worked out that the deciding factor is how often something is needed (every session, no natural trigger vs. only when doing that specific task), not how much content it has — testing/code-style/security/documentation/code-review/convention-levels/upgrading-adoption were already living as pointers in `CLAUDE.md`'s lean dispatch; only session-discipline and the project's own version/sync state genuinely need local reliability, and `AGENTS.md`'s full inlining is a bounded exception for agents that can't fetch on demand at all, not the general model.

- `README.md`: added "Two tiers" under Design, applying the repo's own new "writing for a cold reader" convention since this is exactly the kind of document a cold reader hits first
- `template/conventions/convention-levels.md`: added "How much to keep locally" as the operational counterpart, cross-linked rather than duplicated
- `CHANGELOG.md`: logged `two-tier-locality-model` under Unreleased

Tested the one genuinely untested piece of that model: whether the imperative-fetch pattern (proven for a local path) generalizes to a remote URL, for an adopter with no local agentics clone at all. Built a throwaway fixture whose `CLAUDE.md` pointed only at a raw GitHub URL for `session-discipline.md`, ran a blind subagent against it with no memory of this conversation and no global context of its own.

- Passed first attempt: fetched the real current remote content, derived the correct checklist from it, correctly flagged (rather than silently skipped) that the propagation/contributor-status step needs global context it didn't have, and independently noticed the `justincorrigible` vs `oicr-softeng` URL mismatch without being asked to look for it
- `template/conventions/convention-levels.md`: noted this as tested, not just asserted, in § How much to keep locally
- `CHANGELOG.md`: logged `validate-remote-only-fetch` under Unreleased

A marketing/product framing note from the developer: people adopt agentics, fully or partially; they don't "adopt conventions from agentics" piecemeal. Checked the actual wording rather than assuming it was widespread: only one real instance existed, everywhere else already said "adopted agentics" correctly.

- `template/conventions/convention-levels.md`: fixed the one instance, in § How much to keep locally
- `CONTRIBUTING.md`: added "Adopt agentics, not pieces of it" to § Design principles, so the framing doesn't drift back in future additions
- `CHANGELOG.md`: logged `adopt-agentics-not-pieces` under Unreleased




