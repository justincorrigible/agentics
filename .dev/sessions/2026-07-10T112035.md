Propagated a Dashes convention refinement from the developer's personal global config into the template, closing an ambiguity the template's own text admitted was unresolved.

- `template/conventions/code-style.md`: § Dashes rewritten to explicitly name structural exceptions (bullet markers, horizontal dividers, table separator rows, compound words, numeric ranges) instead of leaving space-hyphen-space vs. bullet-prefix as case-by-case review; added a heading/title example (colon, not dash) that wasn't covered before
- `~/.claude/CLAUDE.md` (outside this repo): same refinement, plus the table-separator addition originated here first; also fixed a stale `sessions.md` reference in the bullet-annotation example to `.dev/sessions/`, found while porting the text over
- `CHANGELOG.md`: logged `dashes-structural-exceptions` under Unreleased

Real confusion while working with an adopted project (Arranger, a different session): the developer relies on git staging to review changes incrementally and discard what doesn't work, expected staging to happen automatically, and hit a moment of "you said there's changes but git shows nothing" when it didn't. Root cause: agents don't stage without being explicitly asked, same as they don't commit, but that constraint was never actually stated anywhere, only "never commit" was.

- `~/.claude/CLAUDE.md` (outside this repo): added explicit staging restraint next to the existing "never commit" rule, plus a requirement to state a change's actual git state (staged vs. unstaged) plainly rather than just confirming it exists
- `template/conventions/code-style.md`: filled in § Git, previously an empty team placeholder, with the same real default guidance rather than leaving it as an example to configure
- `CHANGELOG.md`: logged `git-staging-transparency` under Unreleased
