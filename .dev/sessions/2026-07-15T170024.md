A live adopter's agent (helm-charts) migrated `.dev/sessions.md` into per-day files correctly, but left the old file and a stray `.gitkeep` behind, since nothing in the migration instructions said the cutover was complete once content moved.

- `template/conventions/upgrading-adoption.md`: § 2's migration bullet now states this is a cutover, not an addition: delete `.dev/sessions.md` once every entry has a home in `.dev/sessions/`, and don't leave a placeholder file behind once the directory holds real content
- `CHANGELOG.md`: logged `migration-is-a-cutover` under Unreleased

Applied the same cutover fix directly to a different live adopter (helm-charts): its migration had left `.dev/sessions.md` in place with one stale, superseded draft entry alongside the correctly-migrated `.dev/sessions/` directory, plus a stray staged `.gitkeep`.

- `helm-charts/.dev/sessions.md`: deleted, after confirming its only uncommitted content was an earlier, now-inaccurate draft of a schema change already correctly captured in the migrated per-day file
- `helm-charts/.dev/sessions/.gitkeep`: deleted; flagged to the developer that it was already staged by a prior session, so the deletion itself needs staging separately before a commit will actually drop it

Designed and began implementing auto-versioning: computing agentics' own next semver bump from CHANGELOG entries rather than deciding it by feel.

- `CHANGELOG.md`: header now documents a `bump: patch | minor` field per entry, classified by whether an entry fixes/clarifies/backfills something already intended (patch) versus introduces a rule/file/procedure/section that didn't exist before (minor); a major-worthy change gets flagged to the developer rather than silently downgraded
- `CONTRIBUTING.md`: added § Versioning: `CHANGELOG.md`'s latest dated header is the single source of truth for the current version, not either template file's tag read independently; both template files' tags become stamped copies of it, written together only at an explicit release; documented the rollup rule (highest bump level across all pending entries, not summed) and a bootstrap caveat, since 79 entries already existed under `## Unreleased` before this field did and weren't retroactively classified

A full parity audit across every `CLAUDE.md`/`AGENTS.md` pair, prompted by the third instance of the same drift bug in one session, surfaced a much larger problem than expected: `template/AGENTS.md` had drifted into inlining nearly everything (an entire OWASP catalogue, the full upgrade procedure, most of code-style, several session-discipline sections) that `template/CLAUDE.md` only ever dispatched to, on the premise that `AGENTS.md`'s consumers "cannot fetch a file on demand at all." That premise turned out to be false: the AGENTS.md standard's own guidance recommends dispatch over inlining, since real consumers (Cursor, Copilot, Aider) have file-system access like any other coding agent.

- `template/AGENTS.md`: rebuilt around a single canonical "When to read what" dispatch table, structurally identical to what `template/CLAUDE.md` used to hold, replacing the inlined content entirely
- `template/CLAUDE.md`: "When to read what" now points at `AGENTS.md`'s table instead of keeping its own copy: one table, not two kept in sync by hand
- `CLAUDE.md`, `AGENTS.md` (repo root): applied the same single-source pattern to the much smaller root dispatch list, and fixed both files' "Repo maintenance rules" bullet, which had named only `template/CLAUDE.md`'s tag despite `template/AGENTS.md` carrying its own
- `README.md`, `conventions/convention-levels.md`, `conventions/upgrading-adoption.md`: corrected every remaining claim that `AGENTS.md` must inline content to work standalone

A closer look at the same two files found the dispatch table wasn't the only thing duplicated: Interaction parameters, Critical constraints, Memory hygiene, and Initialization were still verbatim copies in both `template/CLAUDE.md` and `template/AGENTS.md`, the same drift risk at smaller scale.

- `template/CLAUDE.md`, `CLAUDE.md` (repo root): collapsed both to a minimal stub. Each exists only because Claude Code auto-loads it; each now holds nothing but a pointer to its `AGENTS.md` counterpart, which is the sole canonical source for everything, project-specific content included
- `template/AGENTS.md`, `AGENTS.md` (repo root): fixed each file's own self-description, which still told Claude to "prefer CLAUDE.md" — backwards, once CLAUDE.md became a stub pointing back here
- `README.md`, `template/README.md`, `CONTRIBUTING.md`: corrected every remaining place describing the old model: the file tables, the adoption order (`AGENTS.md` is now the required copy, `CLAUDE.md` optional, reversing prior guidance), where project-specific content belongs, the role-file creation steps
- `CHANGELOG.md`: revised the `agents-md-single-dispatch-table` entry in place to cover the full scope (still today, not yet released); logged `changelog-bump-field-and-ssoc` separately under Unreleased

Asked whether other projects' agents would actually see any of today's changes: no, not yet, since nothing here is committed or pushed, and separately, the upstream-check mechanism itself had a real staleness gap once it is.

- `template/conventions/convention-levels.md`: § Checking for upstream updates no longer just "prefers the local Path over the GitHub URL"; it fetches and compares local `HEAD` against the tracked remote branch first, pulls if behind, and falls back to the remote URL if pulling isn't safe. A stale local clone reporting "nothing changed" was worse than the URL, not better
- `CHANGELOG.md`: logged `verify-local-clone-freshness` under Unreleased

Cut agentics' first real release. Classified all 82 pending entries with the new `bump` field (24 patch, 58 minor), computed the rollup (at least one minor present means a single minor bump, 0.1.0 → 0.2.0, not one bump per minor entry), and released.

- `CHANGELOG.md`: renamed `## Unreleased` to `## 0.2.0 - 2026-07-15` once classified; opened a fresh `## Unreleased (next: 0.2.0)` above it for future entries
- `template/CLAUDE.md`, `template/AGENTS.md`: `agentics-template-version` tags stamped to `0.2.0` together, from the same source, not independently
- `CLAUDE.md`, `AGENTS.md` (repo root): `agentics-repo-version` tags also stamped to `0.2.0`. These were previously tracked as a separate version number from the template tags despite sharing one CHANGELOG; unified to always carry the same number going forward, different key names kept for what they each communicate
- `CONTRIBUTING.md` § Versioning: documented the four-tag stamping relationship and marked the bootstrap note resolved, noting future releases should be near-free (bump assigned per entry as it's logged, not a batch project) rather than repeating today's one-time classification pass

Several entries in the backlog (`sessions-per-contributor-file`, `upstream-update-check`, `upgrading-adoption`, `contributor-mandatory-upstream-check`, `agents-md-single-dispatch-table`) read as major-sized structural changes under ordinary semver; flagged rather than silently folded into the mechanical minor bump, per the design, since major stays locked until productization is explicitly authorized.

The developer pointed out the just-cut release still stamped one version number into four independent tags: agreeing today doesn't make them structurally one thing. Redesigned so `CHANGELOG.md` is the only place a version number exists.

- `CHANGELOG.md`: `## Released` now nests dated versions as `###` sub-headings, with `## Unreleased changes` as its own top-level section, rather than both as sibling `##` headings
- `AGENTS.md`, `template/AGENTS.md`: tags became pointers (`see CHANGELOG.md § Released (latest entry)`) instead of stamped numbers
- `CLAUDE.md`, `template/CLAUDE.md`, `template/CLAUDE.softeng.md`: dropped their tags entirely; none of them are the canonical source
- `template/README.md` § Keeping up to date: documented that `template/AGENTS.md`'s pointer only resolves while the file lives in this repo; adoption is now the point where it gets replaced with an actual stamped snapshot, since a copied file can't dynamically reference this repo's `CHANGELOG.md` from somewhere else
- `CONTRIBUTING.md` § Versioning: rewritten to match; dropped the live `(next: X.Y.Z)` header annotation per the developer's preference, rollup is computed on demand instead of maintained as a running value
Folded into `0.2.0` directly rather than left as a separate pending entry, since the developer was about to commit and nothing had shipped yet: merged with the entry it partially contradicted (`changelog-bump-field-and-ssoc`, which still described the four-stamped-tags design) into one entry describing only the final shape, rather than leaving "built X, then immediately undid X" as two entries in the permanent history.
