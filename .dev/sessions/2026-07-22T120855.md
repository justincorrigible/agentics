Following up on two live gaps the developer hit in Lyric: yesterday's no-transient-tech-debt rule only covered an entry resolved before it was written, not one resolved later in the same long session; and a real tech-debt entry there, before and after a manual tightening pass, showed the same restatement-bloat pattern recurring across `.dev/` content generally.

- `template/conventions/session-discipline.md` § Keeping `.dev/` current: added "Re-check before committing, not just at session start", before committing anything touching `.dev/tech-debt.md` or `.dev/roadmap.md`, or code covered by an entry added earlier in the same session, re-verify every such entry against current code state rather than waiting for the next session's staleness pass. Considered pairing this with an exportable git pre-commit hook (mechanical trigger, like agentics' own `.githooks/pre-commit`); developer chose the convention-only fix for now, scoped smaller
- `template/conventions/session-discipline.md`: added § Say it once, at the density it deserves (a fact restated as a bullet after already being said in prose, a blocking condition given its own bolded status section, a standing convention cited by name instead of just applied, all cost the same as stating it once wrong). Folded into the same pre-commit re-check above rather than left as a write-time ideal to remember, the same reasoning that shaped the first fix. `CONTRIBUTING.md`'s `succinct-wording-is-a-separate-pass` now points at this as its canonical statement instead of holding an independent copy
- Asked to check all of today's uncommitted work against the rules just written: found the new "Say it once" section restating its own point twice, and `CONTRIBUTING.md`'s pointer to it doing the same in miniature. Tightened both
- Published `0.8.0`: relocated both `CHANGELOG.md` entries under the new heading, committed, pushed to both `origin` and `softeng-fork`
- Arranger's own upstream-check (run out of date, at the developer's request) surfaced a report on local additions worth evaluating for agentics: `code-style.md` § Dependency version verification gained a sibling case (`npm view <package> dist-tags`, not just `latest`, when evaluating whether a package already has a capability); `documentation.md` § Cross-linking, not duplication gained pointing another repo at a maintained package's own docs instead of writing a bespoke migration explanation. Two other items in the report (search-before-writing, config alphabetization) turned out already covered by current agentics, just invisible to Arranger's stale snapshot
- New `template/CLAUDE.overture.md`: Overture is a product family, not a single repo, so its docs.overture.bio absolute-link rule belongs at the same level as `CLAUDE.softeng.md` (team-wide), not folded into any one Overture repo. Added the dispatch line and initialization question to `AGENTS.md`, the file to `convention-levels.md`'s canonical "never a project copy" list and `template/README.md`. Also added: the cross-project map is expected, not just offered, for Overture projects specifically, since the ecosystem is inherently multi-repo
- `upgrading-adoption.md` § 1: fixed a gap this surfaced in the upgrade path itself: `AGENTS.md` § Initialization only runs for projects with no memory yet, so an already-adopted project would never get asked a question introduced after its initial setup. Now checks every current initialization question against project memory and asks anything unanswered as part of the non-conflicting batch
- Published `0.9.0`: relocated the `overture-product-family-file` entry under the new heading, committed, pushed to both `origin` and `softeng-fork`. Publishing was a prerequisite for the next step (syncing lyric, maestro, docs, infra, helm-charts, usher, and arranger against `CLAUDE.overture.md`): Arranger's own tag already referenced agentics' prior HEAD, but nothing downstream could pick up this feature while it sat uncommitted here
