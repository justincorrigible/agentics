Following up on two live gaps the developer hit in Lyric: yesterday's no-transient-tech-debt rule only covered an entry resolved before it was written, not one resolved later in the same long session; and a real tech-debt entry there, before and after a manual tightening pass, showed the same restatement-bloat pattern recurring across `.dev/` content generally.

- `template/conventions/session-discipline.md` § Keeping `.dev/` current: added "Re-check before committing, not just at session start", before committing anything touching `.dev/tech-debt.md` or `.dev/roadmap.md`, or code covered by an entry added earlier in the same session, re-verify every such entry against current code state rather than waiting for the next session's staleness pass. Considered pairing this with an exportable git pre-commit hook (mechanical trigger, like agentics' own `.githooks/pre-commit`); developer chose the convention-only fix for now, scoped smaller
- `template/conventions/session-discipline.md`: added § Say it once, at the density it deserves (a fact restated as a bullet after already being said in prose, a blocking condition given its own bolded status section, a standing convention cited by name instead of just applied, all cost the same as stating it once wrong). Folded into the same pre-commit re-check above rather than left as a write-time ideal to remember, the same reasoning that shaped the first fix. `CONTRIBUTING.md`'s `succinct-wording-is-a-separate-pass` now points at this as its canonical statement instead of holding an independent copy
- Asked to check all of today's uncommitted work against the rules just written: found the new "Say it once" section restating its own point twice, and `CONTRIBUTING.md`'s pointer to it doing the same in miniature. Tightened both
- Published `0.8.0`: relocated both `CHANGELOG.md` entries under the new heading, committed, pushed to both `origin` and `softeng-fork`
